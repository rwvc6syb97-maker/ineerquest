# 阶段0 · 人工调试待办清单

> 项目:InnerQuest(MBTI 职业规划平台)
> 范围:阶段0(地基 T0-01 ~ T0-11)完成后所有需要人工介入调试/补齐的问题

---

## 一、概述

阶段0(地基)已通过**验证检查点0**:

- 脚手架可正常启动;
- `health` 健康检查全绿;
- 样例接口成功走通 **9 层中间件链**,返回统一响应结构。

本文档列出的条目均为 **"环境非阻断"(blocked)** 项:

- **不影响阶段0门禁**,阶段0 已判定通过;
- 但在**进入阶段1业务开发**或**实际部署**前,需人工补齐相应环境、凭证与业务逻辑。

各条目均对应已核验的真实代码位置,可据此定位并逐项恢复。

---

## 二、A类 · 环境与凭证补齐(最高优先级 · 阻断真实连通)

> 说明:以下凭证/连接串当前均为占位(如 `CHANGE_ME`),导致对应中间件无法真实连通。提供真实实例并回填后,即可解除对应 blocked 项并复验 `health` 探针。

| 项 | 涉及文件 | 占位值 | 恢复方法 | 验证方式 |
| --- | --- | --- | --- | --- |
| **DATABASE_URL(MySQL)** | `deploy/k8s/secret.yaml:14`、`deploy/env/.env.test.example`、`deploy/env/.env.staging.example`、`deploy/env/.env.prod.example`、`apps/api/.env.example` | `CHANGE_ME` | 提供真实 MySQL 实例(字符集 `utf8mb4` / 引擎 `InnoDB`),回填连接串后执行 `npx prisma migrate dev` 建立 37 张表 | `health` 的 `mysql` 探针返回 `true` |
| **REDIS_URL** | 各 `deploy/env/.env.{test,staging,prod}.example`、`apps/api/.env.example` | `CHANGE_ME` | 提供真实 Redis 实例并回填连接串 | `health` 的 `redis` 探针返回 `true` |
| **MONGODB_URI** | 各 `deploy/env/.env.{test,staging,prod}.example`、`apps/api/.env.example` | `CHANGE_ME` | 提供真实 MongoDB 实例并回填连接串 | `health` 的 `mongo` 探针返回 `true` |
| **CLICKHOUSE_PASSWORD** | `deploy/k8s/secret.yaml:16` 及各 env 文件 | `CHANGE_ME` | 提供真实 ClickHouse 实例并回填密码/连接信息 | `health` 的 `clickhouse` 探针返回 `true` |
| **OSS_ACCESS_KEY_ID / OSS_ACCESS_KEY_SECRET** | `deploy/k8s/secret.yaml:20-21` | `CHANGE_ME` | 提供阿里云 OSS 访问密钥对并回填(凭证未设置时 `apps/api/src/infra/oss/oss.service.ts:18` 会跳过初始化) | `health` 的 `oss` 探针返回 `true` |
| **JWT_SECRET** | `deploy/k8s/secret.yaml:18` | `CHANGE_ME` | 生成强随机密钥并回填 | 用于 JWT 签发/校验(配合 B 类鉴权逻辑验证) |
| **LLM_API_KEY** | `deploy/k8s/secret.yaml:23` | `CHANGE_ME` | 配置 LLM 网关密钥(阶段3 必需;MVP 阶段走兜底逻辑) | LLM 调用链路联调 |
| **WECHAT_PAY_MCHID / API_KEY / CERT_SERIAL** | `deploy/k8s/secret.yaml:25-27` | `CHANGE_ME` | 提供微信支付商户资质(付费拓展阶段2 需要) | 支付链路联调 |

---

## 三、B类 · 代码级 TODO(blocked) 业务逻辑待实现

> 说明:以下 9 层中间件链中的关键节点当前为**占位放行**,阶段0 仅验证链路贯通,阶段1 需按下表补齐真实业务逻辑。

| 逻辑 | 文件位置 | 当前状态 | 待实现内容 |
| --- | --- | --- | --- |
| **JWT 鉴权校验** | `apps/api/src/common/guards/auth.guard.ts:25-29` | 仅解析 `Authorization` 头并放行 | 校验 JWT 有效性、注入 `req.user`、处理 401 与刷新令牌 |
| **RBAC 权限比对** | `apps/api/src/common/guards/permission.guard.ts:25-26` | 直接放行 | 从 `req.user` 读取角色/权限,与 `required` 权限比对,不足则抛 403 |
| **限流令牌桶** | `apps/api/src/common/interceptors/rate-limit.interceptor.ts:15-16` | 占位放行 | 基于 Redis `INCR`+`EXPIRE` 或 Lua 令牌桶实现 100/min/user,超限抛 `RATE_LIMITED`(90001) |
| **配额计数** | `apps/api/src/common/interceptors/quota.interceptor.ts:15-16` | 占位放行 | 按路由元数据判定配额类型(报告日限 / AI 轮次 / AI 配额),基于 Redis 日计数器判断,超限抛对应 BizCode |
| **审计落库** | `apps/api/src/common/interceptors/audit.interceptor.ts:24` | 仅记录方法/路径/耗时/traceId | 将写操作审计落库(`event_log` / 审计表) |

---

## 四、C类 · 构建与部署实测(本地环境缺守护进程 · 未实测)

> 说明:以下步骤因本地环境缺少守护进程/集群工具,尚未实测,需在具备条件的环境中复验。

- [ ] **后端构建复验**:`cd apps/api && npm i && npx prisma generate && npx nest build`(此前 `node_modules` 安装遇网络卡顿,需复验)
- [ ] **Docker 构建与编排**:`docker build`(前后端 Dockerfile)+ `docker compose up` 未实测(本地无 docker 守护进程)
- [ ] **K8s 部署**:`kubectl apply -k deploy/k8s` / `--dry-run` 未实测(本地无 kubectl / 集群)
- [ ] **CI/CD 流水线**:`.github/workflows/ci.yml`、`.github/workflows/cd.yml` 未在真实 runner 运行,需补齐:
  - GitHub Secrets:`REGISTRY`、`REGISTRY_USERNAME`、`REGISTRY_PASSWORD`、`KUBECONFIG`
  - 镜像仓库地址
  - Ingress 域名 / TLS / `ingressClassName`(见 `deploy/k8s/ingress.yaml`、`deploy/k8s/api-deployment.yaml:23`、`deploy/k8s/web-deployment.yaml:23`)

---

## 五、D类 · 前端占位页(阶段1 替换)

`apps/web/src/routes.tsx` 中多处 `<Placeholder .../>` 为阶段0 占位,阶段1 需按**前端页面清单**逐页实现替换,涉及页面:

- 首页 / 定价 / 登录 / 注册
- 测评 / 结果
- 概览 / 报告 / 职业规划
- 教练对话 / 运营页

> 系统页 **S01 ~ S05** 已为正式实现,无需替换。

---

## 六、已在验证阶段修复的真实源码缺陷(仅作记录 · 已修复无需处理)

- `schema.prisma`:`updatedAt DateTime` 缺空格 → **已修复**
- `Report.resultId` 一对一关系缺 `@unique` → **已补充**
- `clickhouse.service.ts`:`url` → `host`(适配 `0.2.x` 客户端)→ **已修复**

---

## 七、进入阶段1前的检查清单

> 汇总 A / C 类中进入阶段1业务开发实际需要的前置项:

- [ ] 提供真实 **MySQL 实例**,并执行 `npx prisma migrate dev` 建立 37 张表
- [ ] 提供真实 **Redis 实例**(限流/配额计数依赖)
- [ ] 生成并回填强随机 **JWT_SECRET**
- [ ] 后端 **`npm i` 复验**(确认依赖完整安装,可正常 `prisma generate` / `nest build`)

---

> **恢复条件说明**:一旦满足恢复条件(提供中间件实例及 `DATABASE_URL` 等真实连接凭证并回填),即可解除对应 blocked 项,并复验 `health` 探针全绿。