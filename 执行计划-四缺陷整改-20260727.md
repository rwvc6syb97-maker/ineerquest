# InnerQuest 执行计划 — 四缺陷整改专项

> 版本：v1.0 ｜ 出品：产品经理（PM-Agent）｜ 日期：2026-07-27
> 交付对象：项目调度工程师（rv45nox2）
> 依据：2026-07-27 用户实测 4 缺陷的接口级取证与 PM 裁定（见 memory: project_4bugs_ruling_20260727）

---

## 一、口径裁定基线（全局强制，前后端不得私改）

1. **测评"完成"统一口径**：以 `assessment_record` 表为唯一权威源。
   - started = count(assessment_record WHERE isDeleted=0)
   - submitted = count(assessment_record WHERE status=SUBMITTED(2) AND isDeleted=0)
   - completeRate = submitted / started（无数据兜底 0）
   - 禁止再用 event_log 事件计数作为完成率分子分母（口径漂移根源）。
2. **首页 completedCount 口径**：= submitted（已提交测评记录数），文案标注为"已完成测评"。与后台 submitted 完全一致。
3. **报告数 vs 测评数**：report 表报告数 ≠ 测评提交数（一次提交可不生成报告 / 报告有配额）。后台看板须分列"测评提交数"与"报告生成数"两个独立指标，不得混用。
4. **generateStatus 枚举**（契约 v2.1，不变）：pending / generating / done / failed。前端必须处理全部四态。

---

## 二、缺陷清单与整改任务（按依赖排序）

### 缺陷1 · 深度报告点击后一直不生成
- **根因**：前端触发链路正常；后端 LLM 全降级(degraded=true)时 successCount=0 → report.status=FAILED；生产 Agnes LLM 端点未配好会恒降级，前端只判 done 故表现为"卡住"。
- **任务 1.1【运维/后端】**：核验 Railway 生产环境 LLM 通道（Agnes）配置（LLM_API_URL / KEY / 端点），确认 llm.chat 真实返回 degraded=false，并出具真实回归证据。
- **任务 1.2【前端】**：ReportPage 显式处理 `generateStatus==='failed'`：停止轮询 + 展示"生成失败，重试"按钮；`pending/generating` 保持轮询；`done` 关闭轮询。禁止只判 done。
- **验收**：真实点击深度报告 → 数秒内轮询到 done 并展示 LLM 内容；LLM 异常时 → 落 failed 并可重试，不再无限转圈。

### 缺陷2 · 首页顶部统计数字不真实
- **根因**：前端已调真实 /stats/home 无 mock；根因是口径与用户预期不符。
- **任务 2.1【后端】**：StatsService.completedCount 明确按"§一.2 口径"= assessment_record submitted 计数；补充/校正字段注释；清理 5 分钟缓存以便验证。
- **验收**：首页"已完成测评"数字 = 数据库 assessment_record(status=SUBMITTED) 真实计数，与后台 submitted 一致。

### 缺陷3 · 后台完成率/数量与报告数对不上
- **根因**：admin-analytics.assessmentRate 用 event_log 事件算比率，与 report 表、stats 的 assessment_record 三套口径不统一。
- **任务 3.1【后端】**：assessmentRate 改为按"§一.1 口径"以 assessment_record 表聚合（started/submitted/completeRate），移除 event_log 依赖或仅作趋势展示。
- **任务 3.2【后端】**：后台看板分列"测评提交数(assessment_record)"与"报告生成数(report)"两个指标，标签明确，避免混淆。
- **任务 3.3【数据库】**：校验 assessment_record.status 枚举与 report 表关联字段索引，确保聚合查询性能与口径一致性。
- **验收**：后台完成率 = submitted/started，与首页 completedCount 口径自洽；测评数与报告数分列且各自与 DB 真实计数吻合。

### 缺陷4 · 用户管理缺邮箱 + 最新报告结果 + 各维度数据
- **根因**：AdminUserService.view 无 email，且 list/detail 未关联最新报告与 assessment_result 维度。
- **任务 4.1【后端】**：view 增 `email` 字段，受 user:pii 权限脱敏（无 pii 权限时脱敏，如 a***@b.com）。
- **任务 4.2【后端】**：detail 关联该用户**最新一条报告**：mbtiType、reportNo、reportType、createdAt；及对应 assessment_result 四维度 scoreEi/scoreSn/scoreTf/scoreJp。
- **任务 4.3【数据库】**：确认 user.email、report、assessment_result 关联查询有合适索引（user_id + created_at）。
- **任务 4.4【前端】**：管理后台用户列表补 email 列；用户详情页展示最新 MBTI 类型 + 四维度可视化。
- **验收**：用户详情返回 email（脱敏受权限控制）+ 最新报告 mbtiType + 四维度分数；前端正常渲染。

---

## 三、接口契约变更点（供后端/前端对齐，需 PM 确认后生效）

| 接口 | 变更 | 类型 |
| --- | --- | --- |
| GET /admin/users | list 项新增 email（脱敏） | 新增字段 |
| GET /admin/users/:id | 新增 latestReport{mbtiType,reportNo,reportType,createdAt} 与 dimensions[{dimension,score}] | 新增结构 |
| GET /admin/analytics/assessment-rate | 数据源改 assessment_record，字段 started/submitted/completeRate 不变 | 语义变更 |
| GET /stats/home | completedCount 口径确认为 submitted（字段不变） | 口径确认 |
| POST /reports/:id/generate | 出参不变；前端补 failed 态处理 | 前端行为 |

---

## 四、派工建议（供调度工程师排期）

- **并行组 A（后端为主）**：任务 2.1 / 3.1 / 3.2 / 4.1 / 4.2（后端架构工程师 s19lr0dt）
- **并行组 B（数据库）**：任务 3.3 / 4.3（数据库架构工程师 ltaax899）
- **并行组 C（前端）**：任务 1.2 / 4.4（前端工程师 au-orwo5）
- **串行前置**：任务 1.1（运维/后端核验 LLM）需最先启动，阻塞缺陷1验收。
- **收口**：全部完成后由全栈测试专家（qp52lwfz）按"验收"逐条真实回归，PM 出最终放行。

---

## 五、完成定义（DoD）

1. 四缺陷验收项全部真实回归通过（非 mock）；
2. 三处统计口径自洽（首页 = 后台 submitted，测评数与报告数分列）；
3. 深度报告 done/failed 两态均可正确呈现；
4. 用户管理 email 脱敏合规 + 维度数据展示正常；
5. tsc 零错误，相关单测全绿；PM 出具最终放行结论。