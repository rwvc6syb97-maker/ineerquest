# 我是怎么用 AI 从 0 到 1 做出一个产品的 —— 部门经验分享

> 分享人视角：我几乎没有写代码，却用「AI 多智能体协作 + Loop 编排」把 InnerQuest 从**一句想法**做到了**线上可用**。这篇是我的完整复盘。
> 实战案例：InnerQuest（向内求索）—— MBTI 人格探索与职业规划平台
> 版本：v1.0

---

## 写在前面：我为什么想分享这段经历

做 InnerQuest 之前，我和很多同事一样有几个困惑：

- 我有产品想法，但不会写代码，好像只能等排期、等资源。
- AI 很火，但我一直只拿它写写文案、做做翻译，没想过用它做**真正能上线的产品**。
- 就算让 AI 写代码，我看不懂代码，怎么知道它有没有糊弄我？

这次我真的把一个完整产品做出来并上线了，所以想把过程原原本本讲一遍。我这篇分享想说清楚的其实就一句话：

> **我是怎么把 AI 当成一支「产品团队」来指挥，把想法变成真实可用、质量可控的产品的。**

下面我用 InnerQuest 这个真实做完的产品，从"需求确认"一路讲到"上线"，把我走过的路完整拆给大家看。

---

## 一、先看结果：我最后做出了什么（产品功能介绍）

在讲过程之前，先让大家有个直观印象——我用这套方法，最终交付的是一个**功能完整、前后端 + 数据库齐全、并已部署上线**的产品。

### 1.1 产品是什么

**InnerQuest（向内求索）**：一个 MBTI 人格探索 + 职业规划平台。
一句话定位：**向内剖析人格特质，向外规划长期职业生涯。**

### 1.2 完整功能矩阵（都是真实落地、非 PPT）

| 模块 | 用户能做什么 |
| --- | --- |
| MBTI 测评 | 在线答题 → 生成 16 型人格结果（游客也能答，生成报告时才需登录） |
| 人格报告 | 基于测评结果生成结构化人格报告，分免费/付费章节 |
| 职业推荐 | 根据人格匹配 TOP10 适配职业 |
| 职业百科/详情 | 职业库、职业详情、发展路线图 |
| 技能差距分析 | 分析你与目标职业的技能差距 |
| 学习资源推荐 | 针对差距推荐学习路径 |
| 成长仪表盘/计划 | AI 动态成长计划 |
| AI 对话咨询 | 接入真实大模型，围绕你的报告做个性化问答 |
| 辅导师预约 | 预约真人辅导师、实时会话 |
| 会员/激活码 | 会员体系、激活码兑换 |
| 运营后台 | 题库/用户/套餐/激活码/辅导师/内容/数据看板管理 |
| AI 能力拓展 | 简历生成、协作分析、深度报告章节、模拟面试、职业热点日报等 |

### 1.3 技术上它是"完整的一套"（我不懂技术，但我知道有这些角色）

我做之前才意识到，一个真实产品不是"一个网页"那么简单，它至少包含四个技术工种：

- **前端**：用户看到的页面（网页、按钮、交互）
- **后端**：藏在背后的业务逻辑（登录、算分、下单、权限）
- **数据库**：数据存在哪里（用户、题目、订单、报告）
- **测试**：验证上面三者拼起来是不是真的能用

> **我的关键认知**：过去这四个工种要 4 拨人。而我这次，是让 **AI 分别扮演这 4 个角色（外加产品经理和项目调度）**，由我这个不懂代码的人来"指挥"它们协作。这就是我这次分享的核心。

---

## 二、整体方法论地图（先看我的全景打法）

在讲每一步之前，先给大家看一张我实际用的"地图"。我把整个 0-1 过程浓缩成 **6 个角色 + 1 个循环**：

### 2.1 六个 AI 角色（一支虚拟团队）

```
产品经理 PM     —— 把想法变成清晰的需求和验收标准
   │
项目调度        —— 拆任务、排顺序、分派给谁做、卡质量门
   ├── 数据库工程师  —— 设计数据怎么存
   ├── 后端工程师    —— 实现业务逻辑
   ├── 前端工程师    —— 实现用户界面
   └── 测试工程师    —— 真实验证能不能用
```

### 2.2 一个核心循环：Loop 编排

每一个功能，都不是"一次写完"，而是走一个固定的 5 步循环，任何一步不合格就"打回重做"：

```
建数据库表 → 写后端逻辑 → 做前端页面 → 真实测试 → 产品验收放行
    ↑______________ 不合格就打回上一步 ______________↓
```

### 2.3 全流程一句话串起来

> 我（产品）提想法 → **PM 智能体**帮我写成规范的需求文档 → **调度智能体**把需求拆成一个个 Loop → 每个 Loop 里 **数据库/后端/前端/测试** 四个智能体按顺序接力 → 每步都有"质量门禁"卡关 → 全部通过后 PM 智能体验收放行 → 上线。

**接下来的每一章，我就把这张地图的每一块，结合我实际怎么做的讲透。**

---

## 三、第一步：我是怎么做需求确认的（0 代码也能产出专业 PRD）

这是最关键、也最适合我这种非技术同事发力的一步。**需求没想清楚，后面写得再好都白费。** 我发现 AI 恰好能在这一步帮我补齐"产品专业度"。

### 3.1 我踩过的第一个误区：一上来就让 AI"写代码"

我最初拿到 AI 的第一反应也是「帮我做一个 MBTI 网站」。结果得到的东西就是个玩具，因为**需求太模糊**。后来我改了做法：**先让 AI 扮演产品经理，陪我把需求聊清楚、写成文档。**

### 3.2 我把需求收敛成四层（这是我真实走过的路）

我不是一步到位，而是分四层把想法收敛成可执行的需求：

| 层次 | 产出物 | AI 扮演的角色 | 我做的事 |
| --- | --- | --- | --- |
| ① 市场与定位 | 竞品分析、品牌命名、差异点 | 市场分析师 | 说清"给谁用、解决什么痛点" |
| ② 产品需求 | PRD（功能清单、用户流程图） | 产品经理 | 拍板要哪些功能、优先级 |
| ③ 接口契约 | 接口与字段规范文档 | 产品经理 + 架构师 | 确认"前后端之间怎么对话"的规则 |
| ④ 验收标准 | 每个功能的"什么算做完" | 产品经理 + 测试 | 定义可检验的放行标准 |

> InnerQuest 真实产出：`产品分析报告`（竞品对比+品牌+PRD+流程图）、`PRD-MBTI职业规划网页产品`、`需求文档-接口与测试用例权威规范`。这些都是先由 PM 智能体产出，我审阅拍板。

### 3.3 有三件事我坚持自己拍板

AI 能写文档，但有三件事我坚持必须自己拍板，否则后面全乱：

1. **功能优先级（做什么、不做什么、先做什么）**
   - 例：我中途明确「支付功能先不做，走激活码模式」——这个决策直接砍掉了一整块 Loop。
2. **验收标准（什么算"做完了"）**
   - 我不允许是"能用就行"，要具体到"游客能不能答题""未登录点收藏返回什么提示"。
3. **一条铁律级别的红线（产品的底线约束）**
   - 例：我定了几条铁律——"报告本体严禁 AI 侵入""AI 生成内容必须人工审核后才入库""严禁爬取招聘平台数据"。这些红线写进文档后，所有智能体都必须遵守。

### 3.4 我最看重的一条经验：接口契约要"唯一权威"

产品做到一半我遇到的最大坑是：**后端以为字段叫 A，前端以为叫 B，测试按 C 验**，结果三方对不上，反复返工。

我的解法：**建立一份"接口契约权威基线"文档，规定所有返回结构、错误码、字段命名的唯一标准。** 一旦有分歧，以这份文档为准。

> 真实教训：InnerQuest 早期后端设计文档和实际实现的错误码分段冲突，我后来统一到"契约 v2.0"作为唯一权威，才把混乱收住。**这一步对我这种 0 代码的人尤其管用——我看不懂代码，但我能立一条"这份文档说了算"的规则。**

### 3.5 这一步我总结的经验

- 让 AI 先扮演产品经理，先做竞品分析和产品定位，别急着做功能
- 产出一份 PRD：功能清单 + 优先级 + 用户流程图
- 想清楚"先做什么、不做什么"（我当时果断砍掉了支付）
- 为每个功能写下"可检验的验收标准"
- 建立一份"接口契约权威文档"，声明它是唯一标准
- 写下 3-5 条产品红线（铁律），要求所有后续工作遵守

---

## 四、第二步：我怎么把 AI 变成一支团队（生成智能体并做角色编排）

需求清楚后，我发现光靠"一个通用 AI"来回答问题远远不够，真实产品要分工。所以这一步我做的核心动作是：**为不同工种分别创建"专职智能体"，让每个智能体只干一件擅长的事。**

### 4.1 我为什么用"多个专职智能体"，而不是"一个万能 AI"

打个我当时想通的比方：你不会让一个人同时当医生、律师、会计。原因是——

- **专注度**：给智能体一个明确身份（"你是资深后端架构师"），它的输出质量明显更高、更聚焦。
- **边界感**：数据库智能体不会去乱改前端，职责清晰，出问题好定位。
- **可复用**：这套"团队"配好后，我做下一个产品可以直接复用。

### 4.2 我真实配置的 7 个智能体（角色即身份）

| 智能体 | 身份定位 | 核心职责 |
| --- | --- | --- |
| 产品经理 PM | 需求与验收的权威 | 出 PRD、定契约、做验收裁定 |
| 项目调度 | 总指挥/项目经理 | 拆 Loop、排顺序、分派、卡门禁、回写进度 |
| 数据库架构工程师 | 数据设计专家 | 建表、索引、字段命名、数据隔离 |
| 后端架构工程师 | 业务逻辑专家 | 实现接口、写业务规则、写单元测试 |
| 前端工程师 | 界面交互专家 | 实现页面、对接后端、处理错误态 |
| 全栈测试专家 | 质量守门员 | 真实回归测试、跑测试用例 |
| （通用）编码/问答 | 打杂与答疑 | 临时性小改动、解释说明 |

> 这套角色不是我凭空拍的——它对应真实软件团队的标准分工。**我做的，就是给每个智能体写清楚"你是谁、你负责什么、你不能碰什么"。**

### 4.3 我是怎么"生成"一个专职智能体的（0 代码也能做）

我发现生成智能体本质就是写一段"角色说明书"（专业叫法叫 System Prompt / 自定义指令），我按四要素来写：

1. **角色定义**：你是什么专家（"你是资深数据库架构工程师"）
2. **能力范围**：你能做什么、不能做什么（"你只负责建表和索引，不写业务逻辑"）
3. **行为准则**：怎么干活、遵守哪些红线（"字段命名用下划线、必须软删除、必须做用户数据隔离"）
4. **输出格式**：交付物长什么样（"产出：表结构 + 索引 + 迁移脚本 + 评审结论"）

> 我用的 AI 工具支持"自定义模式/智能体"，我把上面四要素填进去就得到一个专职智能体，全程没写一行代码。

### 4.4 我怎么做角色编排：谁先谁后、谁向谁汇报

有了角色，我还得定"协作关系"。我的编排是一个清晰的层级：

```
        我（产品负责人 / 决策者）
              │  提需求、做拍板
              ▼
        产品经理 PM 智能体
              │  出需求文档 + 验收标准
              ▼
        项目调度智能体（总指挥）
              │  拆成一个个 Loop，按顺序分派
       ┌──────┼──────┬──────┐
       ▼      ▼      ▼      ▼
    数据库 → 后端 → 前端 → 测试   （在��个 Loop 内部按此顺序接力）
       └──────┴──────┴──────┘
              │  测试通过后
              ▼
        产品经理 PM 智能体验收放行
```

### 4.5 我最想强调的一点：一定要有一个"调度智能体"当总指挥

这是我摸索出来、也最容易被忽略但极其重要的一点。**我一开始试着自己逐个指挥 4 个工种智能体，结果被大量技术细节淹没。** 后来我改成：

> 设立一个「**项目调度智能体**」，让它把 PM 的需求拆成一个个可执行的 Loop，决定"先做哪个功能、谁先动手、卡哪些质量门"，并在每个 Loop 完成后回写进度。**我只跟 PM、调度这两个"管理层"智能体对话，技术细节由调度往下派。**

这一下就把我从"技术泥潭"里解放出来——我管方向和验收，调度管执行。

### 4.6 这一步我总结的经验

- 为 6 个核心工种分别创建专职智能体（PM/调度/数据库/后端/前端/测试）
- 每个智能体都写清"角色/能力/准则/输出格式"四要素
- 把产品红线（铁律）写进每个智能体的行为准则里
- 明确协作层级：我 → PM/调度（管理层） → 四个执行工种
- 设立一个"调度智能体"当总指挥，我不直接指挥底层工种

---

## 五、第三步：我怎么用 Loop 编排把功能做出来（这是我打法的"发动机"）

团队搭好后，这一章讲**我是怎么让这支团队高质量地把功能一个个做出来的**。核心就是 **Loop 编排**——这是我整套打法里最值得说的部分。

### 5.1 我理解的 Loop：把大产品拆成"一个个可闭环的小循环"

我没有试图"一次把整个产品做完"。我的做法是：**把产品拆成一个个独立功能（每个功能=一个 Loop），每个 Loop 走完整的 5 步闭环，做完一个再做下一个。**

InnerQuest 我就是这样干的：AI 能力拓展被我拆成 15 个 Loop（如"简历生成""模拟面试""职业热点日报"），技术债清零被拆成 8 个 Loop（L1~L8）。每个 Loop 独立闭环、独立验收。

### 5.2 我给每个 Loop 定的固定 5 步循环

```
Step1 数据库建表评审（数据库智能体）
   → Step2 后端接口实现 + 单元测试（后端智能体）
   → Step3 前端接入 + 联调（前端智能体）
   → Step4 真实回归测试（测试智能体，禁止用假数据）
   → Step5 产品验收放行（PM 登记）
   → 任一步不达标 → 打回上一步重做
```

**这就是我的"发动机"。** 每个功能都塞进这个循环跑一遍，产出质量就稳定了。

### 5.3 我觉得最灵魂的设计：每一步之间的"质量门禁"

光有顺序不够，我发现关键是**每一步之间设一道"门"，不达标不许往下走**。这是我保证质量的核心机制，我真实用的门禁是：

| 流转 | 门禁标准（不满足就打回） |
| --- | --- |
| 建表 → 后端 | 表结构经确认：字段命名规范、有索引、有软删除、做了用户数据隔离 |
| 后端 → 前端 | 后端代码零编译错误 + 单元测试覆盖所有错误码 + 返回字段与契约一致 |
| 前端 → 测试 | 前端字段映射一致 + 无假数据兜底 + 出错时页面显示错误提示而非白屏 |
| 测试 → 验收 | 全部通用测试用例 + 该功能专项用例全部通过 |
| 验收放行 | PM 逐条核对上线放行标准，全达标才放行，否则登记问题打回 |

> **我作为 0 代码的人怎么守门**：我不用看懂代码，但我能问一句"这道门过了没有"。比如我会问测试智能体："断网时页面是显示错误提示还是假数据？" 这就是我在守门。

### 5.4 我最惨痛的教训换来的一条铁律：禁止用"假数据"糊弄

这是我踩坑后总结的最重要一条经验，我想单独讲：

> **联调阶段禁止一切 mock（假数据）掩盖问题。接口失败必须报错、显示错误态，绝不能偷偷用假数据顶上。**

为什么我觉得这条这么重要？因为**假数据是"隐形的定时炸弹"**：
- 开发时用假数据，页面看起来一切正常、很漂亮；
- 真实上线后，接口对不上，用户看到的全是错的，问题在生产环境才爆发。

真实教训：InnerQuest 早期我很多功能"接口失败就自动回退假数据"，导致前后端契约不符被静默掩盖。后来我专门开了一整轮"技术债清零 Loop"（L1~L8）把这些假数据兜底全部清除。**所以我建议这条要在一开始就定为红线，而不是像我一样事后补救。**

### 5.5 我坚持的打回机制：不合格就退回去，别将就

Loop 的价值一半在"打回"。我要求任何一步不达标，就退回上一步重做，绝不带病往下走。例如：
- 后端返回字段和契约文档不一致 → 打回后端重做，不许前端先接；
- 测试发现一个 P0（阻塞级）用例失败 → 整个 Loop 打回，不许放行。

> InnerQuest 的验收裁定里能看到大量"打回—整改—复测"记录，比如某个"生成中死循环""超长内容错误码不对"，都是被测试挡下、打回后端修复、再复测通过才放行的。

### 5.6 我怎么看进度：用文档把每个 Loop 的状态记下来

我让调度智能体每完成一个 Loop，就在编排文档里把状态从「⬜待办」改成「✅完成」，并登记"残留问题"。这样我随时能看到：
- 哪些 Loop 做完了、哪些在做、哪些暂缓；
- 每个 Loop 留了什么尾巴（残留），下一步谁来收。

> 这份"Loop 编排文档"就是我作为产品负责人的**驾驶舱仪表盘**——我不看代码，看它就知道项目到哪了。

### 5.7 这一步我总结的经验

- 把产品拆成一个个独立功能，每个功能 = 一个 Loop
- 每个 Loop 固定走"建表→后端→前端→测试→验收"5 步
- 在每步之间设"质量门禁"，不达标就打回上一步
- 定死一条红线：禁止用假数据兜底，失败必须报错
- 用一份"Loop 编排文档"记录每个 Loop 的状态和残留

---

## 六、第四步：四个工种智能体联动落地实战（我用一个功能走一遍）

理论讲完了，这一章我用一个真实功能——「职业收藏」——完整走一遍，让大家看清我这里四个智能体是怎么"接力"的。这是 InnerQuest 技术债清零里的 L4 Loop。

### 6.1 场景：我要给"职业收藏"做一个完整闭环

需求很简单：用户能收藏喜欢的职业、能取消、能看收藏列表。看似一句话，但我这里要四个工种接力才能真正落地。

### 6.2 Step1｜数据库智能体：先想清楚"数据存哪"

数据库智能体接到需求后，产出：
- 复用已有的"用户收藏表"，增加几个字段（状态、更新时间、删除标记）；
- 加一个"唯一约束"——保证同一个用户不能重复收藏同一个职业；
- 加索引——保证"查我的收藏列表"这个动作快；
- 做用户数据隔离——保证我只能看到我自己的收藏。

> **门禁检查**：字段命名规范？有没有软删除？有没有做隔离？——都达标才放行到后端。

### 6.3 Step2｜后端智能体：实现"业务逻辑"

后端智能体接到过审的表结构，产出三个接口：收藏、取消收藏、查我的收藏列表。并处理各种边界：
- 重复收藏 → 返回"已收藏"的错误码（4403）；
- 取消收藏做"幂等"（重复取消也不报错）；
- 未登录 → 返回鉴权错误码；只能操作自己的数据（强隔离）。

同时**为每个错误码写单元测试**。

> **门禁检查**：代码零编译错误？单测覆盖所有错误码？返回字段和契约一致？——达标才放行到前端。

### 6.4 Step3｜前端智能体：做"用户看得到的界面"

前端智能体接到联调就绪的后端接口，产出：
- 职业列表页、收藏页的"收藏/取消"按钮；
- 收藏状态由服务器决定（不是存在浏览器本地）；
- 加载中、空列表、出错都有界面提示（不白屏）；未登录点收藏 → 引导登录。

特别地，这一步**删掉了旧代码里两个"本地假数据兜底"**，改成真实调用后端。

> **门禁检查**：字段映射对不对？有没有假数据兜底？出错是不是显示错误提示？——达标才放行到测试。

### 6.5 Step4｜测试智能体：真刀真枪验证

测试智能体做"真实回归"（不是看代码，是真的去点、去调）：
- 收藏/取消/列表三接口都成功；重复收藏确实返回 4403；
- 未登录收藏确实被拦、引导登录；换个设备登录收藏还在（证明存服务器而非本地）。

> **门禁检查**：所有用例通过？有一个 P0 失败就打回。

### 6.6 Step5｜PM 智能体：验收放行

PM 逐条核对放行标准，全部达标，登记「✅完成」并记录残留（本例无残留）。至此这个 Loop 闭环。

### 6.7 我从联动里得到的关键经验（都是花钱买来的教训）

1. **字段命名必须三方一致**：数据库用下划线（`user_id`），前端用驼峰（`userId`），中间必须有明确"映射规则"，不统一就对不上。
2. **顺序不能乱**：一定是"数据库先行 → 后端 → 前端 → 测试"。前端不能在后端没就绪时先接（否则接的是空气）。
3. **后端能力缺口要先补**：有些功能前端做不了，是因为后端还没这个接口。我把这类先派后端补齐再接（我这里叫"类别 B"）。
4. **外部依赖单独登记**：像"接入真实大模型 Key""开通消息队列"这类需人工配置的，我单独列台账，不混进自动执行的 Loop（"类别 C"）。
5. **部署也是一个坑**：代码写对不等于能上线。我踩过"部署时漏了一步生成数据库客户端，导致 12 个接口全 404"的坑——**上线后我一定会真实回归一次**。

### 6.8 这一步我总结的经验

- 每个功能都按"数据库→后端→前端→测试→验收"顺序接力
- 明确字段命名的三方映射规则（下划线 ↔ 驼峰）
- 后端能力缺口先补齐，前端再接入
- 外部依赖（大模型 Key、队列等）单独列台账、人工配置
- 上线后务必真实回归一次，别信"部署完就好了"

---

## 七、我作为"零代码"的人，做完这一趟的几点心得

前面讲的是"团队怎么协作"，这一章我想聊聊我自己的感受——一个不会写代码的人，做完整个产品后想通了什么。核心就一句话：**我不需要会写代码，但我需要会"提要求"和"验收"。**

### 7.1 我的角色变了：从"执行者"变成"指挥官"

我最大的转变，是不再想着"自己动手写"，而是把 AI 智能体当成我的团队（产品、后端、前端、测试都有），我只做三件事：
1. **说清楚要什么**（需求）；
2. **定好红线**（哪些绝对不能碰）；
3. **验收结果**（做出来的对不对）。

做完这一趟我最深的体会是：会提要求、会验收，比会写代码更重要。

### 7.2 我实际踩过的三个坑

- **坑一：需求说得太模糊。** 我一开始说"做个测评"太空，AI 只能猜。后来我学会说清"多少题、答完给什么结果、结果长什么样"。**越具体，返工越少。**
- **坑二：被"假数据"骗过。** AI 为了让页面"看起来能用"，常会填假数据兜底。我曾经看着好像成了，其实后端根本没通。后来我定死红线：宁可报错，不许用假数据糊弄。
- **坑三：以为"部署完就好了"。** 代码跑通 ≠ 线上能用。我就吃过"上线后一批接口全挂"的亏。现在我每次上线后，都会自己去真实点一遍。

### 7.3 我常用的一个"提要求模板"

我给 AI 派活，基本都套这个结构：

> 我要做【什么功能】。用户会【怎么操作】，然后系统应该【返回什么结果】。
> 要注意这些边界：【没登录怎么办 / 重复操作怎么办 / 出错怎么提示】。
> 红线：【禁止用假数据兜底，失败必须报错】。
> 做完后请告诉我：【怎么验证它真的成了】。

套上这个模板后，我明显感觉 AI 的产出质量上了一个台阶。

### 7.4 我验收时会问自己的四个问题

1. 是不是**真的**调了后端（而不是假数据）？
2. 出错时有没有**明确提示**（而不是白屏或假装成功）？
3. 换个账号 / 换台设备，数据还对不对（证明存的是服务器而非本地）？
4. 上线后**我亲手点过一遍**了吗？

我要四个都"是"，才敢算真的完成。

### 7.5 这一趟我最想留下的几句话

- 把自己定位成"指挥官"：说清要求 + 定红线 + 做验收
- 需求尽量具体，减少 AI 猜测
- 每个任务都写明红线"禁止假数据兜底"
- 用一个固定的"提要求模板"给 AI 派活
- 上线后一定亲手真实回归一遍

---

## 八、总结：我这一趟走下来的方法论

最后我把整趟经历浓缩成一张图，也是我以后再做产品会继续沿用的打法：

### 8.1 我走的五个阶段一句话总览

| 阶段 | 我做的事 | 我交给谁 |
|---|---|---|
| ① 需求确认 | 把想法讲成"具体的功能清单" | 产品智能体 |
| ② 生成与编排 | 组建智能体团队、定好协作顺序 | 项目调度智能体 |
| ③ Loop 编排 | 按"5 步循环 + 质量门禁"推进 | 全体智能体 |
| ④ 四工种落地 | 数据库→后端→前端→测试，逐个功能接力 | 四个工种智能体 |
| ⑤ 验收上线 | 真实回归、上线后再点一遍 | 测试 + 我本人 |

### 8.2 我用血泪换来的五条"铁律"

1. **契约唯一**：字段和错误码以一份权威文档为准，别各写各的。
2. **禁止假数据兜底**：失败必须报错，不许用假数据糊弄。
3. **命名三方一致**：数据库、后端、前端字段要能对上。
4. **每步设门禁**：不达标就打回，别把问题往后传。
5. **上线必回归**：部署完不等于能用，亲手验一遍。

### 8.3 如果我重来一遍，会怎么起步

1. **先练提要求**：用 7.3 那个模板，先拿三个小功能练手。
2. **再练验收**：用 7.4 的四个问题逐条对照。
3. **最后练编排**：把多个功能串成 Loop，用一份文档记录进度和残留。

### 8.4 我最想跟大家说的几句

- 我最大的感受是"角色转变"：不会写代码从来不是障碍，会指挥和验收才是关键。
- 「职业收藏」那个案例是我印象最深的，因为它把四工种接力讲得最清楚。
- 五条铁律里，我反复栽跟头最多的就是"禁止假数据兜底"，希望大家别重蹈我的覆辙。

---

> 结语：产品从 0 到 1，靠的不再是"一个人熬夜写代码"，而是"一个人指挥一支 AI 团队"。我负责想清楚要什么、守住红线、验收结果；剩下的交给智能体。这，就是我这一趟做下来对 AI 时代做产品的理解。