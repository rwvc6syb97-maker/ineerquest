# 需求文档｜全功能免费化改造（取消付费门禁）

> 出品：产品经理（PM-Agent）｜日期：2026-07-29｜版本：v1.0
> 本文为权威规范。字段命名、返回结构、错误码一经确定，前端/后端/数据库不得私自修改，变更须经产品确认。

---

## 一、业务需求简述

**目标**：将 InnerQuest 现有「付费/会员专享」功能全部改为**免费开放**，所有登录用户（部分场景含游客）均可无门槛使用完整能力。

**范围内（本次改造）**
1. 报告完整段落：取消「付费段落 = 未解锁」门禁，报告一律返回全部段落。
2. AI 会员专享 4 项能力：AI 成长计划、AI 简历生成、AI 模拟面试、AI 面试题库评分 —— 取消会员校验，全部放行。
3. 前端付费墙/会员引导/开通会员入口/支付下单入口的隐藏或降级。

**范围外（保留但停用）**
- 会员套餐、激活码、支付订单相关**表结构与接口保留**（不删表、不删接口），仅**不再作为功能门禁**，以便后续可回滚为付费。
- 后台套餐管理、激活码管理保留。

**核心原则**
- 只改「判定逻辑」，不改「数据结构」与「返回字段结构」，保证前后端契约不破坏、可回滚。
- 免费化为**服务端权威**：即便前端漏改，后端也必须放行；前端仅做体验清理。

---

## 二、全局强制约定（不可私改）

1. 后端统一返回结构：`{ code: number; message: string; data: any }`（沿用契约 v2.0）。
2. 错误码 `AI_MEMBER_ONLY(4515)`、`REPORT_LOCKED(4302)` **枚举保留**，但改造后**正常业务流程中不得再抛出**（仅保留定义以防回滚/兜底）。
3. 字段命名：接口/前端驼峰，数据库下划线，ORM 映射不变。
4. 时间：后端存 UTC，接口返回北京时间字符串，沿用现状。
5. 免费化开关建议以**代码常量/配置**承载（如 `FREE_MODE=true`），便于一键回滚，禁止散落魔法值。

---

## 三、改造点清单（按 Agent 归属）

### 后端（backend-agent，权威改造）
| 编号 | 位置 | 现状 | 改造后 |
|---|---|---|---|
| B1 | `report.service.ts` `getReportForOwner`（~448/451/460） | `isUnlocked===1` 才返回付费段，否则仅预览段/置 content=null | 视为恒解锁，返回全部段落 content |
| B2 | `report.service.ts` 概览出参（~511/578） | `isUnlocked` 按库值 + `lockedSectionKeys` 有值 | `isUnlocked` 恒 true、`lockedSectionKeys` 恒空数组 |
| B3 | `report.service.ts` `getSection`（~706/712） | 付费段未解锁抛 `REPORT_LOCKED(4302)` | 直接返回段落，不再抛 4302 |
| B4 | `report.service.ts` 导出（~929） | `isUnlocked!==1` 抛 4302 | 允许导出 |
| B5 | `ai-career-plan.service.ts` `ensureMember`（~87） | 非会员抛 `AI_MEMBER_ONLY(4515)` | 直接放行 |
| B6 | `ai-resume.service.ts` `ensureMember`（~116 相关） | 同上 | 直接放行 |
| B7 | `ai-interview.service.ts`（~214） | 同上 | 直接放行 |
| B8 | `interview-bank.service.ts`（~124） | 题库评分非会员抛 4515 | 直接放行 |

> 约束：B1~B4 只改判定，**不改 `isUnlocked` 字段本身语义与类型**；对外 `isUnlocked` 恒返回 `true`。B5~B8 建议统一由 `FREE_MODE` 常量控制，`ensureMember` 早返回。

### 前端（frontend-agent，体验清理）
| 编号 | 位置 | 改造后 |
|---|---|---|
| F1 | `report.api.ts` / 报告详情页 | 移除/隐藏「解锁全部段落」付费墙，直接展示全部段落 |
| F2 | `ResumeGenerateBlock.tsx` | 移除「会员专享/开通会员」引导，直接可用 |
| F3 | `InterviewPracticeBlock.tsx` | 同上，评分与参考答案直接展示 |
| F4 | `CareerAiToolsTabs.tsx` | 移除「会员专享」副标题文案 |
| F5 | `usePayment.ts` / 报告解锁调用 | 不再触发报告解锁支付流程 |
| F6 | 会员/套餐入口（顶栏、报告页 CTA、路由 P09 等） | 隐藏「开通会员」按钮，避免死入口 |

> 约束：前端**不得删除** membership.api / payment.api 模块本身（保留以备回滚），仅隐藏入口与门禁分支。

### 数据库（db-agent，校验为主）
| 编号 | 内容 |
|---|---|
| D1 | 不删除 membership/plan/order/activation_code/report.is_unlocked 等结构 |
| D2 | 校验：改造后新生成报告 `is_unlocked` 无论 0/1，接口层均按已解锁返回，确认无遗留读取 is_unlocked 作为过滤条件的 SQL |
| D3 | 校验：确认无触发器/视图依赖 membership_level 做数据过滤 |

---

## 四、异常场景与错误码

| 场景 | 期望行为 |
|---|---|
| 未登录访问报告付费段 | 仍走登录校验（401/未登录码），登录后全段可见 |
| 游客调用 AI 会员能力 | 按原鉴权规则（需登录则 401），登录后放行，**不再返回 4515** |
| 报告生成中访问 | 仍返回 `REPORT_GENERATING(4303)`，与免费化无关 |
| 报告不存在/越权 | 仍返回 `4203/ASSESSMENT_RECORD_NOT_FOUND`，权限隔离不变 |
| 历史已存在付费订单/会员用户 | 不受影响，正常使用（本就有权限） |
| 4515 / 4302 | 正常流程**禁止再出现**；若出现视为改造缺陷 |

> 权限隔离：普通用户仅能访问**本人**报告与数据，越权仍拦截；免费化**不放开数据越权**。

---

## 五、上线验收测试用例清单

### A. 报告免费化
- TC-A1：新用户完成测评生成报告，`GET /reports/:id` 返回 `isUnlocked=true`、`lockedSectionKeys=[]`、全部段落 content 非 null。
- TC-A2：`GET /reports/:id?sectionKey=<付费段>` 直接返回内容，不抛 4302。
- TC-A3：报告导出接口对未支付用户直接成功。
- TC-A4：前端报告详情页无付费墙、无「解锁全部」按钮，全段可读。

### B. AI 能力免费化
- TC-B1：非会员账号调用 AI 成长计划 → 200，不返回 4515。
- TC-B2：非会员调用 AI 简历生成 → 200。
- TC-B3：非会员调用 AI 模拟面试 start/答题 → 200，评分可见。
- TC-B4：非会员调用面试题库评分 → 200，参考答案可见。
- TC-B5：前端职业详情页 AI 工具区无「会员专享/开通会员」文案与拦截。

### C. 回归与隔离
- TC-C1：未登录调用需鉴权接口仍返回未登录错误码（鉴权不放松）。
- TC-C2：用户 A 访问用户 B 的报告仍被拦截（数据越权隔离不变）。
- TC-C3：报告生成中仍返回 4303，报告不存在仍返回 4203。
- TC-C4：会员套餐/激活码/支付接口本身仍可正常返回（未删除）。
- TC-C5：`tsc` 零错误、后端单测全绿（含原 4515/4302 断言用例已同步调整为放行语义）。

### 放行标准（上线验收）
1. TC-A/B/C 全部通过；
2. 正常流程中不再出现 4515、4302；
3. 鉴权与数据越权隔离保持不变；
4. 前后端返回字段结构与类型一致、可回滚；
5. 后端单测/tsc 全绿。

---

## 六、Loop 循环执行计划（交付调度 Agent 指挥）

> 调度 Agent 按下列 Loop 顺序派工，每个 Loop 完成即回归验收，未过不进入下一 Loop。

- **Loop 0｜基线冻结**：调度 Agent 确认改造清单（三、四章）与验收用例（五章），建立 `FREE_MODE` 免费化开关约定。产出：改造范围确认。
- **Loop 1｜后端报告免费化（B1~B4）**：backend-agent 改报告服务判定，`isUnlocked` 恒真、`lockedSectionKeys` 恒空、不再抛 4302。产出：接口自测通过 TC-A1~A3。
- **Loop 2｜后端 AI 能力免费化（B5~B8）**：backend-agent 关闭 4 个 `ensureMember`，统一走 `FREE_MODE`。产出：TC-B1~B4 接口自测通过。
- **Loop 3｜后端单测/tsc 收口（C5）**：backend-agent 同步调整原 4515/4302 断言用例为放行语义，tsc 零错、单测全绿。
- **Loop 4｜数据库校验（D1~D3）**：db-agent 校验无残留 is_unlocked/membership 过滤 SQL，结构未删。产出：DB 校验报告。
- **Loop 5｜前端体验清理（F1~F6）**：frontend-agent 隐藏付费墙/会员引导/开通入口，保留 api 模块。产出：TC-A4、TC-B5 通过。
- **Loop 6｜全栈联调回归**：test-agent 按第五章全量执行 TC-A/B/C，重点验证正常流程零 4515/4302、鉴权与越权隔离不变。产出：回归报告。
- **Loop 7｜PM 终审放行**：PM 依放行标准核验，全部通过则出上线放行结论；如残留缺陷回退对应 Loop。

**协作交付链**：PM 需求文档（本文）→ backend-agent（Loop1-3）→ db-agent（Loop4）→ frontend-agent（Loop5）→ test-agent（Loop6）→ PM 终审（Loop7）。