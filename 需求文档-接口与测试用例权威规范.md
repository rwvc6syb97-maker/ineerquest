# InnerQuest 向内求索 — 需求文档（接口 / 入参出参 / 错误码 / 测试用例）

> **产品**：InnerQuest（向内求索）— MBTI 人格探索与职业规划平台
> **文档定位**：本文件为「数据库 / 后端 / 前端」三端开发的**唯一权威来源**。字段命名一经确定，三端不得私自修改，变更须经产品确认。
> **技术栈**：NestJS 10 + React 18 + Prisma + MySQL 8.0（纯 MySQL，无 Redis）
> **大模型**：Agnes AI（OpenAI 兼容，baseURL=`https://apihub.agnes-ai.com/v1`，model=`agnes-2.0-flash`）
> **版本**：v2.0 · **日期**：2026-07-09 · **状态**：权威基线（对齐当前落地）

---

## 目录

1. [全局强制约定](#1-全局强制约定)
2. [错误码枚举总表](#2-错误码枚举总表)
3. [通用异常场景矩阵](#3-通用异常场景矩阵)
4. [模块 1：用户账户](#4-模块-1用户账户)
5. [模块 2：MBTI 测评](#5-模块-2mbti-测评)
6. [模块 3：人格报告](#6-模块-3人格报告)
7. [模块 4：职业推荐](#7-模块-4职业推荐)
8. [模块 5：AI 对话（SSE）](#8-模块-5ai-对话sse)
9. [模块 6：激活码兑换](#9-模块-6激活码兑换)
10. [模块 7：辅导预约](#10-模块-7辅导预约)
11. [模块 8：后台管理](#11-模块-8后台管理)
12. [上线验收测试用例清单](#12-上线验收测试用例清单)

---

## 1. 全局强制约定

### 1.1 统一返回结构（不可变更）

```ts
interface ApiResponse<T = unknown> {
  code: number;      // 业务码，见 §2
  message: string;   // 人类可读提示
  data: T;           // 业务数据；无数据时为 null
}
```

- **HTTP 状态码**：与业务码语义同步（200/400/401/403/404/409/429/500）。
- **列表 data 结构**：`{ list: T[]; total: number; page: number; pageSize: number }`。
- **成功**：`code=200`；`4xxx`=客户端错误；`5xxx`=服务端异常；`9xxx`=限流/系统。

### 1.2 code 设计规则

| 段 | 含义 |
|----|------|
| `200` | 成功 |
| `40xx` | 通用/鉴权客户端错误 |
| `41xx` | 用户账户 |
| `42xx` | 测评 |
| `43xx` | 报告 |
| `44xx` | 职业推荐 |
| `45xx` | AI 对话 |
| `46xx` | 激活码/会员 |
| `47xx` | 辅导预约 |
| `48xx` | 后台管理 |
| `50xx` | 服务端异常 |
| `90xx` | 限流/系统繁忙 |

### 1.3 命名规范（三端强制一致）

- **接口字段 / 前端**：`camelCase`（驼峰）。
- **数据库字段**：`snake_case`（下划线），由 Prisma `@map` 做映射转换。
- **路由路径**：小写短横线，资源名复数；C 端前缀 `/api/v1`，后台前缀 `/api/v1/admin`。
- **示例映射**：接口 `userId` ↔ DB `user_id`；接口 `createdAt` ↔ DB `created_at`。

### 1.4 时间规范（权威裁定 v2.2，统一 ISO8601 UTC）

- 后端内部与 DB 统一存 **UTC**（`DATETIME`）。
- **接口返回统一为 ISO8601 UTC 字符串**（带 `Z` 后缀）：`YYYY-MM-DDTHH:mm:ss.sssZ`，如 `2026-07-10T03:20:00.000Z`。
- **弃用**旧约定的"北京时间字符串 `YYYY-MM-DD HH:mm:ss`"作为接口返回格式。
- **前端负责本地化展示**：由前端统一 util 将 ISO8601 UTC 转东八区展示，不再依赖后端下发本地化字符串。
- **修复归属（P2 可迭代）**：
  - 后端：报告 `createdAt` 等原返回北京时间字符串的字段，统一改为 ISO8601 UTC；测评 `startedAt`/`completedAt`、支付 `expireAt` 已为 UTC，保持 `.sssZ` 完整格式。
  - 前端：所有时间字段接入统一 `formatDateTime(utcStr)` 做时区换算展示。

### 1.5 鉴权与请求头

- `Authorization: Bearer <accessToken>`。
- **双 Token**：`accessToken` 有效期 30 分钟，`refreshToken` 30 天。
- **黑名单**：登出 / 封禁后写入 MySQL `cache_kv` 表（`key=blacklist:{jti}`，TTL 由 `expire_at` 控制），无 Redis。
- `X-Trace-Id`（可选）全链路追踪。

### 1.6 限流策略（MySQL 令牌桶）

- 全局：**100 次/分钟/用户**，超限返回 `code=9001` + HTTP 429 + `Retry-After` 头。
- 计数落在 MySQL `cache_kv` / `cache_zset` 表，滑动窗口按 `key=rate:{scope}:{id}`。
- 特殊接口单独更严格：短信验证码 1 次/60s；登录 5 次/分；AI 对话按日配额。

### 1.7 数据存储替代 Redis 约定

| 原 Redis 用途 | MySQL 替代方案 |
|--------------|---------------|
| 键值缓存 / JWT 黑名单 | `cache_kv(key, value, expire_at)` |
| 排队 / 令牌桶 / 限流窗口 | `cache_zset(key, member, score)` |
| 答题草稿断点续答 | `assessment_progress` 表 |
| 分布式锁（辅导时段） | MySQL 行锁 `SELECT ... FOR UPDATE` + 唯一约束 |

### 1.8 幂等约定

- 所有写接口支持 `Idempotency-Key` 请求头。
- 激活码兑换按 `code_no` 唯一约束幂等；辅导下单按 `(userId, scheduleId)` 唯一约束幂等。

---

## 2. 错误码枚举总表

> 所有接口共用此表；模块章节内仅列该模块专属码。`message` 为默认文案，前端可覆盖。

### 2.1 通用/鉴权（40xx）

| code | message | 触发条件 | HTTP |
|------|---------|---------|------|
| 200 | success | 请求成功 | 200 |
| 4000 | 参数校验失败 | 入参缺失/类型错误/格式不合法 | 400 |
| 4001 | 参数超长 | 字段超过最大长度限制 | 400 |
| 4002 | 请求体为空 | body 为空或非法 JSON | 400 |
| 4010 | 未登录或登录已过期 | 无 Token / accessToken 失效 | 401 |
| 4011 | Token 无效 | 签名错误 / 已加入黑名单 | 401 |
| 4012 | refreshToken 失效 | 刷新令牌过期或被吊销 | 401 |
| 4030 | 无权限访问 | 越权 / 角色不足 | 403 |
| 4040 | 资源不存在 | 目标记录不存在或已软删除 | 404 |
| 4090 | 重复提交 | 幂等键命中 / 唯一约束冲突 | 409 |

### 2.2 服务端与系统（50xx / 90xx）

| code | message | 触发条件 | HTTP |
|------|---------|---------|------|
| 5000 | 系统内部错误 | 未捕获异常 | 500 |
| 5001 | 数据库操作失败 | DB 连接/事务异常 | 500 |
| 5002 | 第三方服务调用失败 | Agnes AI/短信/OSS 调用异常 | 502 |
| 5003 | 上游服务超时 | 第三方响应超时 | 504 |
| 9001 | 请求过于频繁 | 命中限流令牌桶 | 429 |
| 9002 | 系统繁忙，请稍后重试 | 排队/降级 | 503 |

### 2.3 业务专属码索引

| 段 | 模块 | 详见 |
|----|------|------|
| 41xx | 用户账户 | §4 |
| 42xx | MBTI 测评 | §5 |
| 43xx | 人格报告 | §6 |
| 44xx | 职业推荐 | §7 |
| 45xx | AI 对话 | §8 |
| 46xx | 激活码/会员 | §9 |
| 47xx | 辅导预约 | §10 |
| 48xx | 后台管理 | §11 |

---

## 3. 通用异常场景矩阵

> 每个接口在实现与测试时**必须**覆盖以下边界，除非明确标注不适用。

| 场景 | 预期行为 | 统一码 |
|------|---------|--------|
| 空输入 | 校验拦截，提示具体缺失字段 | 4000 |
| 格式错误 | 手机号/邮箱/枚举值不合法即拦截 | 4000 |
| 参数超长 | 超字段上限拦截 | 4001 |
| 重复提交 | 幂等键或唯一约束返回上次结果或冲突 | 4090 |
| 登录凭证过期 | 401，前端触发 refresh；refresh 也失效则跳登录 | 4010/4012 |
| 越权访问 | 校验资源归属/角色，拒绝 | 4030 |
| 第三方失败 | 捕获降级，不透传上游堆栈 | 5002 |
| 上游超时 | 设置超时阈值，超时即返回 | 5003 |
| 网络中断 | 前端重试；写接口靠幂等键去重 | — |
| 并发请求 | 行锁/唯一约束/乐观锁保证一致 | 4090 |
| 频繁调用限流 | 令牌桶拦截 | 9001 |
| 软删除逻辑 | `is_deleted=1` 记录对 C 端不可见 | 4040 |
| 权限隔离 | 普通用户/管理员接口物理隔离，`/admin/*` 需管理员 | 4030 |

---

## 4. 模块 1：用户账户

**功能**：手机验证码 + 邮箱密码双通道登录、双 Token 刷新、黑名单登出、资料/隐私管理、注销冷静期。

### 4.1 接口清单

| # | 方法 | 路由 | 说明 | 鉴权 | 限流 |
|---|------|------|------|------|------|
| 1 | POST | `/api/v1/auth/sms/send` | 发送短信验证码 | 否 | 1 次/60s/手机号 |
| 2 | POST | `/api/v1/auth/login/sms` | 手机验证码登录（无则注册） | 否 | 5 次/分 |
| 3 | POST | `/api/v1/auth/login/email` | 邮箱密码登录 | 否 | 5 次/分 |
| 4 | POST | `/api/v1/auth/register/email` | 邮箱注册 | 否 | 3 次/分 |
| 5 | POST | `/api/v1/auth/refresh` | 刷新 Token | refreshToken | - |
| 6 | POST | `/api/v1/auth/logout` | 登出（加黑名单） | 是 | - |
| 7 | GET | `/api/v1/users/me` | 当前用户资料 | 是 | - |
| 8 | PATCH | `/api/v1/users/me` | 更新资料 | 是 | - |
| 9 | GET | `/api/v1/users/me/privacy` | 获取隐私设置 | 是 | - |
| 10 | PATCH | `/api/v1/users/me/privacy` | 更新隐私设置 | 是 | - |
| 11 | POST | `/api/v1/users/me/deactivate` | 申请注销（30 天冷静期） | 是 | - |

### 4.2 入参 / 出参

**① 发送验证码** `POST /auth/sms/send`
- 入参：`phone` string 必填（大陆手机号正则 `^1[3-9]\d{9}$`）；`scene` string 必填（`login`/`bind`）。
- 出参：`{ expireIn: number }`（验证码有效秒数，默认 300）。

**② 手机验证码登录** `POST /auth/login/sms`
- 入参：`phone` string 必填；`code` string 必填（6 位数字）。
- 出参：`{ accessToken, refreshToken, user: UserProfile, isNewUser: boolean }`。

**③ 邮箱密码登录** `POST /auth/login/email`
- 入参：`email` string 必填（邮箱格式）；`password` string 必填（8~32 位）。
- 出参：同 ②（无 `isNewUser`）。

**④ 邮箱注册** `POST /auth/register/email`
- 入参：`email` 必填；`password` 必填（8~32，含字母+数字）；`nickname` 可选（≤20）。
- 出参：`{ userId, email }`。

**⑤ 刷新 Token** `POST /auth/refresh`
- 入参：`refreshToken` string 必填。
- 出参：`{ accessToken, refreshToken }`（轮换刷新）。

**⑦ UserProfile 结构**（`GET /users/me` 的 data）
```ts
{ userId: number; userNo: string; nickname: string; avatar: string;
  phone: string;      // 脱敏 138****8000
  email: string;      // 脱敏
  role: number;       // 1 普通 / 2 辅导师 / 3 管理员
  membershipLevel: number; // 0 免费 / 1 Pro / 2 辅导
  membershipExpireAt: string | null;
  createdAt: string; }
```

### 4.3 专属错误码（41xx）

| code | message | 触发条件 |
|------|---------|---------|
| 4101 | 验证码错误或已过期 | code 不匹配/超时 |
| 4102 | 验证码发送过于频繁 | 60s 内重复发送 |
| 4103 | 账号或密码错误 | 邮箱登录凭证不符 |
| 4104 | 邮箱已注册 | 注册时唯一约束冲突 |
| 4105 | 账号已被封禁 | `user.status=2` |
| 4106 | 账号处于注销冷静期 | 已申请注销未撤销 |
| 4107 | 密码强度不足 | 不满足 8~32 位含字母数字 |

### 4.4 异常场景与测试用例

| 用例 | 输入 | 预期 |
|------|------|------|
| 正常-验证码登录 | 有效 phone+正确 code | 200，返回双 Token，新用户 isNewUser=true |
| 正常-邮箱登录 | 正确 email+password | 200，返回双 Token |
| 异常-手机号格式 | phone=`123` | 4000 |
| 异常-验证码错误 | code=`000000` | 4101 |
| 异常-频繁发码 | 60s 内二次发送 | 4102 |
| 异常-密码错误 | 错误 password | 4103 |
| 异常-邮箱重复注册 | 已存在 email | 4104 |
| 异常-封禁登录 | status=2 用户 | 4105 + HTTP403 |
| 异常-弱密码注册 | password=`12345678` | 4107 |
| 异常-未登录取资料 | 无 Token 调 `/users/me` | 4010 |
| 异常-Token 过期 | 过期 accessToken | 4010，refresh 后重放成功 |
| 异常-refresh 失效 | 过期 refreshToken | 4012，跳登录 |
| 边界-昵称超长 | nickname 21 字符 | 4001 |
| 幂等-重复注册请求 | 同 Idempotency-Key | 返回首次结果 |

---

## 5. 模块 2：MBTI 测评

**功能**：40 题（4 页×10 题）→ 计算 16 型人格 → 生成结果；支持断点续答。题库按 `question_version` 隔离。

### 5.1 接口清单

| # | 方法 | 路由 | 说明 | 鉴权 |
|---|------|------|------|------|
| 1 | GET | `/api/v1/assessments/questions` | 拉取当前版本题库（40 题） | 是 |
| 2 | POST | `/api/v1/assessments/records` | 创建测评记录（开始） | 是 |
| 3 | PATCH | `/api/v1/assessments/records/:id/answers` | 分页暂存答案（断点续答） | 是 |
| 4 | POST | `/api/v1/assessments/records/:id/submit` | 提交答卷，计算 MBTI 类型 | 是 |
| 5 | GET | `/api/v1/assessments/records/:id/result` | 获取测评结果 | 是 |
| 6 | GET | `/api/v1/assessments/records` | 我的测评历史（分页） | 是 |
| 7 | GET | `/api/v1/assessments/records/latest` | 获取最近未完成记录（续答入口） | 是 |

### 5.2 入参 / 出参

**① 题库** `GET /api/v1/assessments/questions` → data（权威裁定 v2.2，三方以此为准，字段名/类型/结构不得私改）：

```ts
interface QuestionBank {
  /** 题库版本号，如 'v1' */
  version: string;              // 必填
  /** 题目总数（如 40），供前端 length/分页计算 */
  total: number;               // 必填
  /** 扁平题目数组，已按 dimension→sortOrder 全局排序；前端直接 slice 分页 */
  questions: Array<{
    /** 题目主键，字符串（BigInt 序列化，避免精度丢失） */
    id: string;                // 必填
    /** 维度字符串（后端将内部 number 编码映射为字符串下发） */
    dimension: 'EI' | 'SN' | 'TF' | 'JP';  // 必填
    /** 题干文案 */
    content: string;           // 必填
    /** 全局排序序号，供稳定分页 */
    sortOrder: number;         // 必填
    /** 是否反向计分题（后端计分用，前端仅透传） */
    isReverse: boolean;        // 必填
    /** 选项数组，已按 sortOrder 排序 */
    options: Array<{
      /** 选项主键，字符串；作答提交时作为 optionId 上送 */
      id: string;              // 必填
      /** 选项标识键，如 'A'/'B'（后端计分辅助） */
      optionKey: string;       // 必填
      /** 选项展示文案（前端直接展示，等价于旧 label） */
      content: string;         // 必填
      /** 极性，如 'E'/'I'（后端计分用，前端不用） */
      polarity: string;        // 必填
      /** 该选项得分（后端计分用，前端不用） */
      score: number;           // 必填
      /** 选项排序序号 */
      sortOrder: number;       // 必填
    }>;
  }>;                          // 必填
}
```

**裁定说明（三方对齐指令，P0 阻断上线）：**
- **结构裁定：扁平数组**。`questions` 为全局排序后的扁平 `Array`，**弃用**后端按维度分组的 `dimensions: {EI:[],SN:[],TF:[],JP:[]}` 对象结构。理由：QuizPage 依赖 `questions.slice()`/`questions.length` 分页答题，扁平顺序是核心链路刚需。
- **dimension 字段裁定：字符串** `'EI'|'SN'|'TF'|'JP'`，**弃用** number 编码对外暴露。后端在组装时按 `DimensionKey` 将内部 number 映射为字符串下发。前端据此取维度主题色与展示。
- **option 命名裁定**：权威字段为 `{ id, optionKey, content, polarity, score, sortOrder }`。**弃用**前端 `{ value, label }`。展示文案统一用 `content`（等价旧 label）；作答提交时上送 `optionId = option.id`。`polarity`/`score`/`optionKey` 由后端计分使用，前端仅存在不消费（本地评分已随 T2 移除）。
- **修复归属**：
  - **后端**：将 `getQuestions` 返回结构由分组对象 `{version,total,dimensions}` 改为扁平 `{version,total,questions:[]}`；`dimension` number→字符串映射后下发。
  - **前端**：`Question` 类型改为 `{ id, dimension, content, sortOrder, isReverse, options:[{id,optionKey,content,polarity,score,sortOrder}] }`；`getQuestions` 返回类型由 `Question[]` 改为 `QuestionBank`，取用 `resp.questions`；QuizPage 将 `opt.label`→`opt.content`，移除 `opt.value` 依赖。

**② 创建记录** 入参：`questionVersion` string 必填。出参：`{ recordId: number; recordNo: string; startedAt: string }`。

**③ 暂存答案** 入参：
- `page` number 必填（1~4）；`answers` 数组必填，元素 `{ questionId: number; optionId: number }`。
- 出参：`{ savedCount: number; answeredTotal: number }`。

**④ 提交** 出参：
```ts
{ recordId: number; mbtiType: string; // 如 'ENFJ'
  dimensionScores: { EI: number; SN: number; TF: number; JP: number };
  resultId: number; reportId: number | null }
```

**⑤ 结果** `GET /api/v1/assessments/records/:id/result` → data（权威裁定 v2.2，P1）：

```ts
interface AssessmentResult {
  resultId: string;            // 必填，字符串（BigInt 序列化）
  recordId: string;            // 必填
  mbtiType: string;            // 必填，如 'ENFJ'
  /** 四维度得分数组，结构与报告概览 dimensions 一致，供雷达图复用 */
  dimensions: Array<{
    dimension: 'EI' | 'SN' | 'TF' | 'JP';
    left: string;              // 左极标签，如 '内向 I'
    right: string;             // 右极标签，如 '外向 E'
    score: number;             // 0-100
  }>;                          // 必填，固定 4 项
  summary: string;             // 必填，摘要文案
  completedAt: string;         // 必填，ISO8601 UTC（见时间格式裁定）
}
```

**裁定说明（P1）：**
- **字段名裁定：`dimensions`**（数组，结构对齐报告概览 `ReportOverview.dimensions`），**弃用**后端 `scores{EI..}` 与旧文档 `dimensionScores{EI..}` 的对象映射形式。理由：结果页与报告页共用雷达图组件，统一结构避免二次转换。
- **提交接口④** 出参 `dimensionScores`（对象形式）**保留**用于提交瞬时回显，但结果/报告持久化查询统一用 `dimensions` 数组结构。
- **修复归属**：后端将结果查询出参由 `scores` 改为 `dimensions` 数组；前端 `AssessmentResult.dimensions` 由 `Record<Dimension,{...}>` 改为数组形式。

### 5.3 专属错误码（42xx）

| code | message | 触发条件 |
|------|---------|---------|
| 4201 | 题库版本已失效 | 提交时版本与当前不符 |
| 4202 | 答卷不完整 | 缺题或某维度题量不达标 |
| 4203 | 测评记录不存在 | recordId 无效/非本人 |
| 4204 | 测评已提交，不可修改 | 对已完成记录暂存/重复提交 |
| 4205 | 选项与题目不匹配 | optionId 不属于该 questionId |

### 5.4 异常场景与测试用例

| 用例 | 输入 | 预期 |
|------|------|------|
| 正常-完整测评 | 40 题全答→提交 | 200，返回 mbtiType |
| 正常-题库结构 | 拉取 questions | 200，data.questions 为扁平数组，length=40，dimension 为字符串，option 含 content |
| 正常-断点续答 | 分页暂存后重新拉取 latest | 恢复已答进度 |
| 异常-答卷不完整 | 仅答 30 题提交 | 4202 |
| 异常-版本失效 | 用旧 version 提交 | 4201 |
| 异常-越权访问记录 | 访问他人 recordId | 4030/4203 |
| 异常-已提交再暂存 | 对完成记录 PATCH | 4204 |
| 异常-选项不匹配 | optionId 跨题 | 4205 |
| 边界-page 越界 | page=5 | 4000 |
| 并发-重复提交 | 同记录并发 submit | 首次成功，余 4204/4090 |
| 幂等-网络重试提交 | 同 Idempotency-Key | 返回首次结果 |

---

## 6. 模块 3：人格报告

**功能**：分章节深度解读；**免费预览 + 付费完整**。含三段 LLM 生成：深度性格 / 职业建议 / 人际关系。付费解锁依赖激活码升级 Pro。

### 6.1 接口清单

| # | 方法 | 路由 | 说明 | 鉴权 |
|---|------|------|------|------|
| 1 | GET | `/api/v1/reports/:reportId` | 报告概览（含解锁状态） | 是 |
| 2 | GET | `/api/v1/reports/:reportId/sections` | 章节列表（免费/付费标记） | 是 |
| 3 | GET | `/api/v1/reports/:reportId/sections/:sectionKey` | 章节详情（付费章节需已解锁） | 是 |
| 4 | POST| `/api/v1/reports/:reportId/generate` | 触发 LLM 生成三段深度内容 | 是 |
| 5 | POST | `/api/v1/reports/:reportId/share` | 生成分享海报 | 是 |

### 6.2 入参 / 出参

**① 报告概览** `GET /api/v1/reports/:reportId` → data（权威裁定 v2.1，三方以此为准，字段名/类型/必填性不得私改）：

```ts
interface ReportOverview {
  /** 报告主键，字符串（BigInt 序列化，避免精度丢失） */
  id: string;                 // 必填
  /** 关联测评记录 id，供"重新查看/重测"跳转 */
  recordId: string;           // 必填
  /** 报告编号，如 RPT-20260709-XXXX */
  reportNo: string;           // 必填
  /** MBTI 四字母类型，如 'INTJ' */
  mbtiType: string;           // 必填
  /** 族群，用于主题色/文案：由 mbtiType 推导后端下发，前端不再自行推导 */
  family: 'analyst' | 'diplomat' | 'sentinel' | 'explorer';  // 必填
  /** 摘要文案，纯字符串（后端由结构化 summary 渲染成文案下发，前端直接展示） */
  summary: string;            // 必填
  /** 四维度得分，供雷达图；score 为 0-100 或后端约定区间 */
  dimensions: {
    dimension: 'EI' | 'SN' | 'TF' | 'JP';
    left: string;             // 左极标签，如 '内向 I'
    right: string;            // 右极标签，如 '外向 E'
    score: number;
  }[];                        // 必填，固定 4 项
  /** 是否已解锁付费深度内容 */
  isUnlocked: boolean;        // 必填
  /** 章节对象数组：未解锁时仅含免费预览章节，付费章节内容不下发 */
  sections: {
    sectionKey: string;
    title: string;
    content: object;          // 结构化内容对象
    sortOrder: number;
    paid: boolean;
  }[];                        // 必填（可为空数组）
  /** 未解锁的付费章节 key 列表；已解锁时为 [] */
  lockedSectionKeys: string[];  // 必填
  /** 深度生成状态全集（后端将内部数字枚举映射为下列字符串） */
  generateStatus: 'pending' | 'generating' | 'done' | 'failed';  // 必填
  /** 创建时间，ISO8601 UTC 字符串（见 1.4 时间规范；前端本地化展示） */
  createdAt: string;          // 必填
}
```

**裁定说明（三方对齐指令）：**
- **弃用** `previewSections`/`lockedSections` 双 key 数组；概览统一返回 `sections`（对象数组，含预览内容）+ `lockedSectionKeys`（string[]）。
- **状态字段**统一用 `generateStatus`（字符串枚举），**弃用**后端顶层 `status`（数字枚举）对外暴露。映射规则：内部 `GENERATING(0)→'generating'`、`READY(1)→'done'`、`FAILED(2)→'failed'`；报告已创建但深度内容尚未触发生成时下发 `'pending'`。
- 后端需在概览接口顶层**新增组装** `recordId`、`family`（由 mbtiType 推导）、`summary`（渲染为 string）、`dimensions`（取自 dimension_scores 章节四维得分）。前端**不得**再从 sections 内部反解这些字段。
- 前端 `generateStatus` 类型需补齐 `'pending'`，并去除对 sections 反解 family/dimensions 的逻辑。

**③ 章节详情** data：
```ts
{ sectionKey: string; title: string; isFree: boolean;
  content: string;  // Markdown，LLM 生成
  charts?: object }  // 维度雷达等结构化数据
```

**④ 触发生成** 入参：`sections` string[]（可选，默认全部三段 `deep_personality`/`career_advice`/`relationship`）。出参：`{ generateStatus, taskId }`。

### 6.3 专属错误码（43xx）

| code | message | 触发条件 |
|------|---------|---------|
| 4301 | 报告未生成 | 结果尚未产出报告 |
| 4302 | 章节未解锁 | ��问付费章节但未升级 Pro |
| 4303 | 报告生成中 | 重复触发生成 |
| 4304 | 报告生成失败 | LLM 调用失败，可重试 |
| 4305 | 章节不存在 | sectionKey 非法 |

### 6.4 异常场景与测试用例

| 用例 | 输入 | 预期 |
|------|------|------|
| 正常-免费预览 | 访问 preview 章节 | 200，返回内容 |
| 正常-Pro 看完整 | Pro 用户访问付费章节 | 200 |
| 异常-未付费访问 | 免费用户访问付费章节 | 4302 |
| 异常-报告未生成 | 结果未产出 | 4301 |
| 异常-LLM 失败 | Agnes AI 超时 | 4304，前端可重试 |
| 异常-重复生成 | 生成中再次�发 | 4303 |
| 异常-非法章节 | sectionKey=`xxx` | 4305 |
| 异常-第三方降级 | Agnes 502 | 5002，不透传堆栈 |
| 边界-越权看他人报告 | 他人 reportId | 4030 |

---

## 7. 模块 4：职业推荐

**功能**：基于人格类型输出 TOP10 匹配职业；含职业百科、路线图、技能差距、学习资源。

### 7.1 接口清单

| # | 方法 | 路由 | 说明 | 鉴权 |
|---|------|------|------|------|
| 1 | GET | `/api/v1/careers/recommendations` | 基于我的 MBTI 的 TOP10 匹配 | 是 |
| 2 | GET | `/api/v1/careers` | 职业百科列表（搜索/分类/分页） | 否 |
| 3 | GET | `/api/v1/careers/:careerId` | 职业详情 | 否 |
| 4 | GET | `/api/v1/careers/:careerId/roadmap` | 发展路线图 | 是 |
| 5 | GET | `/api/v1/careers/:careerId/skill-gap` | 技能差距分析（基于用户档案） | 是 |
| 6 | GET | `/api/v1/careers/:careerId/resources` | 关联学习资源 | 否 |
| 7 | POST | `/api/v1/careers/:careerId/favorite` | 收藏职业 | 是 |
| 8 | DELETE | `/api/v1/careers/:careerId/favorite` | 取消收藏 | 是 |

### 7.2 入参 / 出参

**① 推荐** 入参 Query：`reportId` number 可选（默认取最新报告）。data：
```ts
{ mbtiType: string;
  list: Array<{ careerId: number; name: string; category: string;
    matchScore: number;   // 0~100
    salaryRange: string; outlook: string; matchReason: string }> }  // 固定 10 条
```

**② 职业列表** 入参 Query：`keyword` 可选（≤50）；`category` 可选；`page`/`pageSize`。data：列表结构（§1.1）。

**⑤ 技能差距** data：
```ts
{ careerId: number;
  gaps: Array<{ skillName: string; requiredLevel: number; userLevel: number; gap: number }>;
  recommendResources: Array<{ resourceId: number; title: string; type: string; url: string }> }
```

### 7.3 专属错误码（44xx）

| code | message | 触发条件 |
|------|---------|---------|
| 4401 | 无可用测评结果 | 用户未完成测评，无法推荐 |
| 4402 | 职业不存在 | careerId 无效/已下架 |
| 4403 | 已收藏 | 重复收藏 |
| 4404 | 技能差距需登录并完成测评 | 缺档案数据 |

### 7.4 异常场景与测试用例

| 用例 | 输入 | 预期 |
|------|------|------|
| 正常-推荐 TOP10 | 已完成测评用户 | 200，list 长度 10 |
| 正常-游客查百科 | 无 Token 访问列表/详情 | 200 |
| 异常-未测评推荐 | 无测评结果 | 4401 |
| 异常-职业已下架 | 下架 careerId | 4402（软删除不可见） |
| 异常-重复收藏 | 已收藏再收藏 | 4403 |
| 异常-未登录收藏 | 无 Token | 4010 |
| 空态-搜索无结果 | keyword 无匹配 | 200，list=[]，total=0 |
| 边界-keyword 超长 | 51 字符 | 4001 |
| 边界-分页越界 | page 超总页 | 200，list=[] |

---

## 8. 模块 5：AI 对话（SSE）

**功能**：SSE 流式多轮对话；单会话 **50 轮上限** + **日配额**；上下文超阈值做摘要压缩。大模型 Agnes AI（agnes-2.0-flash）。

### 8.1 接口清单

| # | 方法 | 路由 | 说明 | 鉴权 | 限流 |
|---|------|------|------|------|------|
| 1 | POST | `/api/v1/ai/conversations` | 创建会话 | 是 | - |
| 2 | GET | `/api/v1/ai/conversations` | 我的会话列表（分页） | 是 | - |
| 3 | GET | `/api/v1/ai/conversations/:id/messages` | 历史消息（分页） | 是 | - |
| 4 | POST | `/api/v1/ai/conversations/:id/messages` | 发送消息（SSE 流式返回） | 是 | 日配额 |
| 5 | GET | `/api/v1/ai/quota` | 查询今日剩余配额 | 是 | - |
| 6 | DELETE | `/api/v1/ai/conversations/:id` | 删除会话（软删除） | 是 | - |

### 8.2 入参 / 出参

**① 创建会话** 入参：`title` 可选（≤30）；`scene` 可选（`report`/`career`/`free`）。出参：`{ conversationId, conversationNo, round: 0 }`。

**④ 发送消息（SSE）** 入参：`content` string 必填（≤2000）。
- **响应**：`Content-Type: text/event-stream`，逐帧推送：
```
event: message
data: {"delta":"你好","messageId":123}

event: done
data: {"messageId":123,"round":5,"tokensUsed":320,"remainQuota":45}
```
- 错误帧：`event: error\ndata: {"code":4501,"message":"超出每日配额"}`。

**⑤ 配额** data：`{ dailyLimit: number; used: number; remain: number; resetAt: string }`。

### 8.3 专属错误码（45xx）

| code | message | 触发条件 |
|------|---------|---------|
| 4501 | 超出每日对话配额 | used ≥ dailyLimit |
| 4502 | 会话已达 50 轮上限 | round ≥ 50 |
| 4503 | 会话不存在 | id 无效/非本人 |
| 4504 | 消息内容超长 | content > 2000 |
| 4505 | AI 生成失败 | Agnes AI 返回异常 |
| 4506 | AI 响应超时 | 上游超时（映射 5003） |

### 8.4 异常场景与测试用例

| 用例 | 输入 | 预期 |
|------|------|------|
| 正常-流式对话 | 有效 content | SSE 逐帧 delta + done 帧 |
| 正常-上下文压缩 | 超阈值多轮 | 自动摘要，语义连贯 |
| 异常-超配额 | used≥limit | error 帧 4501 |
| 异常-超 50 轮 | round=50 再发 | 4502 |
| 异常-内容超长 | 2001 字符 | 4504 |
| 异常-会话越权 | 他人会话 id | 4030/4503 |
| 异常-AI 超时 | Agnes 无响应 | error 帧 4506，前端提示重试 |
| 异常-网络中断 | SSE 断连 | 前端重连，已存消息不丢 |
| 边界-空消息 | content=`` | 4000 |
| 限流-高频发送 | 秒级连发 | 9001 |

---

## 9. 模块 6：激活码兑换

**功能**：会员套餐；激活码兑换升级 Pro / 辅导。无在线支付，仅激活码通道。

### 9.1 接口清单

| # | 方法 | 路由 | 说明 | 鉴权 |
|---|------|------|------|------|
| 1 | GET | `/api/v1/memberships/plans` | 会员套餐列表 | 否 |
| 2 | POST | `/api/v1/memberships/redeem` | 兑换激活码 | 是 |
| 3 | GET | `/api/v1/memberships/me` | 我的会员状态 | 是 |
| 4 | GET | `/api/v1/memberships/records` | 我的兑换记录 | 是 |

### 9.2 入参 / 出参

**① 套餐列表** data：`{ list: Array<{ planId, name, level, durationDays, benefits: string[] }> }`。

**② 兑换** 入参：`code` string 必填（激活码，去空格，长度固定，如 16 位）。
- 出参：
```ts
{ redeemId: number; planId: number; level: number;   // 1 Pro / 2 辅导
  membershipExpireAt: string; message: string }
```
- 幂等：按 `code_no` 唯一约束；同码重复兑换返回 4602。

**③ 我的会员** data：`{ level, expireAt, isActive }`。

### 9.3 专属错误码（46xx）

| code | message | 触发条件 |
|------|---------|---------|
| 4601 | 激活码无效 | 不存在/格式错误 |
| 4602 | 激活码已被使用 | 已核销 |
| 4603 | 激活码已过期 | 超有效期 |
| 4604 | 激活码已作废 | 被后台禁用 |
| 4605 | 当前会员等级更高，无需降级兑换 | 兑换低等级码 |

### 9.4 异常场景与测试用例

| 用例 | 输入 | 预期 |
|------|------|------|
| 正常-兑换 Pro | 有效未用码 | 200，等级升级，返回到期时间 |
| 异常-无效码 | 乱码 | 4601 |
| 异常-已用码 | 已核销码 | 4602 |
| 异常-过期码 | 超期码 | 4603 |
| 异常-作废码 | 后台禁用码 | 4604 |
| 异常-未登录兑换 | 无 Token | 4010 |
| 并发-同码双端兑换 | 同码并发 | 首次成功，余 4602/4090 |
| 边界-code 带空格 | ` code ` | trim 后正常兑换 |
| 幂等-重复请求 | 同 Idempotency-Key | 返回首次结果 |

---

## 10. 模块 7：辅导预约

**功能**：匹配规划师、预约时段。时段并发用 **MySQL 分布式锁**（`SELECT ... FOR UPDATE` + `coach_schedule` 唯一约束）保证不超卖。

### 10.1 接口清单

| # | 方法 | 路由 | 说明 | 鉴权 |
|---|------|------|------|------|
| 1 | GET | `/api/v1/coaches` | 规划师列表（筛选/分页） | 是 |
| 2 | GET | `/api/v1/coaches/:id` | 规划师详情 | 是 |
| 3 | GET | `/api/v1/coaches/:id/schedules` | 可预约时段 | 是 |
| 4 | POST | `/api/v1/coaching/orders` | 创建预约订单（锁时段） | 是 |
| 5 | GET | `/api/v1/coaching/orders` | 我的预约（分页） | 是 |
| 6 | POST | `/api/v1/coaching/orders/:id/cancel` | 取消预约 | 是 |
| 7 | POST | `/api/v1/coaching/orders/:id/review` | 提交评价 | 是 |

### 10.2 入参 / 出参

**③ 可预约时段** data：`{ list: Array<{ scheduleId, date, startTime, endTime, status }> }`（status：`available`/`booked`）。

**④ 创建预约** 入参：`coachId` number 必填；`scheduleId` number 必填。
- 出参：`{ orderId, orderNo, coachId, scheduleId, status: 'booked', appointmentAt }`。
- 幂等：`(userId, scheduleId)` 唯一约束；并发抢同一时段仅一人成功。

**⑦ 评价** 入参：`rating` number 必填（1~5）；`comment` 可选（≤500）。

### 10.3 专属错误码（47xx）

| code | message | 触发条件 |
|------|---------|---------|
| 4701 | 时段已被占用 | 并发抢占，锁失败 |
| 4702 | 规划师停止接单 | `coach.status≠1` |
| 4703 | 会员权限不足 | 非辅导会员预约 |
| 4704 | 订单不存在 | id 无效/非本人 |
| 4705 | 订单状态不允许该操作 | 已完成订单取消等 |
| 4706 | 已评价，不可重复 | 重复提交评价 |
| 4707 | 时段不存在或已过期 | scheduleId 失效 |

### 10.4 异常场景与测试用例

| 用例 | 输入 | 预期 |
|------|------|------|
| 正常-预约成功 | 辅导会员选可用时段 | 200，status=booked |
| 并发-抢同一时段 | 双用户同 scheduleId | 一人 200，另一人 4701 |
| 异常-规划师停接单 | status≠1 | 4702 |
| 异常-非辅导会员 | 免费/Pro 用户预约 | 4703 |
| 异常-取消已完成 | 已完成订单取消 | 4705 |
| 异常-重复评价 | 二次评价 | 4706 |
| 异常-越权取消 | 他人订单 | 4030/4704 |
| 异常-过期时段 | 已过期 scheduleId | 4707 |
| 边界-rating 越界 | rating=6 | 4000 |
| 幂等-重复下单 | 同 (userId,scheduleId) | 返回首次订单 |

---

## 11. 模块 8：后台管理

**功能**：题库 / 用户 / 内容 / 激活码 / 数据分析 / 运营�前缀 `/api/v1/admin`，全部经 `RequireAdmin` + 权限点校验（RBAC）。普通用户访问一律 4030。

### 11.1 接口清单

| # | 方法 | 路由 | 说明 | 权限点 |
|---|------|------|------|--------|
| 1 | POST | `/api/v1/admin/auth/login` | 后台登录（scope=admin） | - |
| 2 | GET/POST/PUT/DELETE | `/api/v1/admin/questions` | 题库 CRUD | question:read/write |
| 3 | PATCH | `/api/v1/admin/questions/batch-status` | 批量上下架 | question:write |
| 4 | GET | `/api/v1/admin/users` | 用户检索 | user:read |
| 5 | PATCH | `/api/v1/admin/users/:id/status` | 封禁/解封 | user:ban |
| 6 | GET/POST/PUT/DELETE | `/api/v1/admin/careers` | 职业库 CRUD | career:read/write |
| 7 | GET/POST/PUT/DELETE | `/api/v1/admin/resources` | 学习资源 CRUD | resource:read/write |
| 8 | GET/POST | `/api/v1/admin/activation-codes` | 激活码列表/批量生成 | code:read/write |
| 9 | PATCH | `/api/v1/admin/activation-codes/:id/disable` | 作废激活码 | code:write |
| 10 | GET | `/api/v1/admin/analytics/overview` | 数据看板核心指标 | analytics:read |
| 11 | GET | `/api/v1/admin/analytics/funnel` | 转化漏斗 | analytics:read |

### 11.2 入参 / 出参（关键）

**⑧ 批量生成激活码** 入参：`planId` 必填；`count` 必填（1~1000）；`expireAt` 必填。出参：`{ batchNo, generated: number, codes: string[] }`。

**⑤ 封禁** 入参：`status` 必填（1 正常 / 2 禁用）；`reason` 必填（≤200）。封禁后写黑名单强制下线。

**⑩ 看板** data：`{ totalUsers, dau, mau, paidUsers, payRate, totalRevenue, todayNew }`。

### 11.3 专属错误码（48xx）

| code | message | 触发条件 |
|------|---------|---------|
| 4801 | 后台账号或密码错误 | 登录失败 |
| 4802 | 权限点不足 | RBAC 校验失败 |
| 4803 | 目标记录被引用，无法删除 | 题目关联进行中测评 |
| 4804 | 批量数量超限 | count>1000 |
| 4805 | 敏感操作需二次确认 | 缺 confirm 标记 |

### 11.4 异常场景与测试用例

| 用例 | 输入 | 预期 |
|------|------|------|
| 正常-管理员登录 | 正确凭证 | 200，scope=admin Token |
| 异常-普通用户访问后台 | 非管理员 Token | 4030 |
| 异常-权限点不足 | 缺 user:ban 调封禁 | 4802 |
| 正常-批量生成激活码 | count=100 | 200，返回 100 码 |
| 异常-超量生成 | count=2000 | 4804 |
| 异常-删除被引用题目 | 关联测评 | 4803 |
| 异常-封禁缺理由 | reason 为空 | 4000 |
| 异常-封禁未二次确认 | 缺 confirm | 4805 |
| 审计-写操作记录 | 任一写操作 | event_log 记录操作人/IP/前后值 |
| 越权-手机号明文 | 无 user:pii | 返回脱敏号 |

---

## 12. 上线验收测试用例清单

> 全部用例通过方可上线。分「冒烟」「回归」「安全/权限」「性能/限流」四类。

### 12.1 冒烟主链路（P0，必过）

| # | 场景 | 通过标准 |
|---|------|---------|
| S1 | 注册/登录（手机+邮箱双通道） | 返回双 Token，`/users/me` 可用 |
| S2 | 完整测评 40 题→提交 | 生成正确 mbtiType + 报告 |
| S3 | 断点续答 | 恢复已答进度不丢 |
| S4 | 报告免费预览 | 免费章节可见，付费章节锁定 |
| S5 | 激活码兑换 Pro→解锁完整报告 | 升级后付费章节可读 |
| S6 | 职业 TOP10 推荐 | 返回 10 条含 matchScore |
| S7 | AI 流式对话 | SSE 逐帧返回 + done 帧 |
| S8 | 辅导预约（辅导会员） | 时段锁定成功 |

### 12.2 异常回归（P0）

| # | 场景 | 通过标准 |
|---|------|---------|
| R1 | 各接口空输入/格式错误 | 统一 4000 |
| R2 | 参数超长 | 4001 |
| R3 | 未登录访问受保�接口 | 4010 |
| R4 | Token 过期→refresh→重放 | refresh 后成功；refresh 失效 4012 |
| R5 | 越权访问他人资源 | 4030 |
| R6 | 软删除记录对 C 端不可见 | 4040 |
| R7 | Agnes AI 超时/失败降级 | 5002/5003，不透传堆栈 |

### 12.3 并发与幂等（P0）

| # | 场景 | 通过标准 |
|---|------|---------|
| C1 | 并发提交同一测评 | 仅一次生效，余 4204/4090 |
| C2 |同码并发兑换 | 仅一人成功，余 4602 |
| C3 | 并发抢同一辅导时段 | 仅一人成功，余 4701 |
| C4 | 网络重试带 Idempotency-Key | 返回首次结果，不重复写 |

### 12.4 安全与限流（P0）

| # | 场景 | 通过标准 |
|---|------|---------|
| SEC1 | 封禁用户强制下线 | 黑名单命中，4011/4105 |
| SEC2 | 后台接口普通用户访问 | 4030 |
| SEC3 | 手机号/邮箱返回脱敏 | 无 pii 权限见脱敏 |
| SEC4 | 全局限流 100 次/分 | 超限 9001 + Retry-After |
| SEC5 | 短信 1 次/60s、登录 5 次/分 | 超限拦截 |
| SEC6 | AI 日配额/50 轮上限 | 4501/4502 |
| SEC7 | 生产环境无敏感信息泄露 | 错误响应不含堆栈/连接串 |

### 12.5 一致性验收（上线门禁）

- [ ] 接口入参、返回字段、数据类型三端完全一致（对照本文档）��
- [ ] DB 字段下划线 ↔ 接口驼峰，Prisma 映射无遗漏。
- [ ] 时间返回 ISO8601 UTC 字符串（带 Z），DB 存 UTC，前端本地化展示。
- [ ] 所有异常场景均有对应错误码且被处理。
- [ ] 权限校验完备，`/admin/*` 全量 RBAC。
- [ ] Agnes AI 可正常调用且失败可降级。
- [ ] 全部 P0 用例通过。

---

> **交付流程**：本文档 → 数据库 Agent 校验表结构 → 后端 Agent 实现接口 → 前端 Agent 对接 → 全栈测试专家执行 §12 用例 → 通过后上线。字段变更须回本文档更新并经产品确认。