# 阶段2 · 人工调试待办清单

> 项目:InnerQuest(MBTI 职业规划平台)
> 范围:阶段2(报告解锁与支付 V1.1 · T2-01 ~ T2-07)完成后所有需要人工介入调试/补齐的问题

---

## 一、概述

阶段2(报告解锁与支付 V1.1)核心后端链路已实现并通过门禁:

- **L1 编译**:`npx tsc -p tsconfig.json --noEmit` 全绿(TSC_OK);
- **L2 单测**:`payment.service.spec.ts` 13/13 通过,覆盖多态下单、超时关单、回调幂等与签名、退款状态机、mock 渠道适配器;
- 交付链路:多态下单(报告解锁/会员) → 15分钟延迟关单 → 发起支付 → 回调幂等验签 → 报告解锁 → PDF 导出 → 退款申请。

本文档列出的条目均为 **"环境非阻断"(blocked / mock)** 项:

- **不影响阶段2门禁**,阶段2 已判定通过;
- 但在**真实联调**或**实际部署**前,需人工补齐相应外部资质、中间件实例与真实接口。

各条目均对应已核验的真实代码位置,联调补齐后即可删除对应的降级兜底逻辑。

---

## 二、A类 · 外部资质与凭证(最高优先级 · 阻断真实连通)

> 说明:以下能力当前均走 mock / 兜底 / 占位实现。提供真实凭证与实例并回填后,即可解除对应 blocked 项并复验真实链路。

| 项 | 涉及文件 | 占位或开关 | 恢复方法 | 验证方式 |
| --- | --- | --- | --- | --- |
| **微信支付商户资质** | `apps/api/src/modules/payment/pay-channel.adapter.ts`(`WechatPayAdapter`,`enabled = WECHAT_PAY_ENABLED === 'true'`,默认 `false` 走 mock) | 开关 `WECHAT_PAY_ENABLED`(默认 `false`) | 置 `true`,接入微信支付 APIv3,回填商户号 / APIv3 密钥 / 商户证书 / 平台证书 | 真实微信支付下单返回可跳转的 `prepay` 参数 |
| **微信支付回调验签** | `apps/api/src/modules/payment/pay-channel.adapter.ts`(`verifySign` 当前为 HMAC-SHA256 mock 复算比对) | mock 签名密钥 `mockKey` | 替换为微信 APIv3 平台证书 SHA256-RSA 验签,校验 `Wechatpay-Signature` 请求头 | 真实微信回调通过验签,非法签名 `70007` |
| **支付渠道退款(真实)** | `apps/api/src/modules/payment/pay-channel.adapter.ts`(`refund` 当前 mock 直接返回成功) | mock 渠道退款 | 接入微信支付退款 APIv3,回填退款请求 | 真实退款到账,退款单置 `SUCCESS` |
| **PDF 中文字体** | `apps/api/src/modules/report/report.service.ts`(`buildMinimalPdf` 零依赖手写 Helvetica,非 ASCII 字符降级为 `?` 标 blocked) | 内置 Helvetica Type1,无中文字体嵌入 | 嵌入中文 TrueType 字体子集(或改用 pdfkit / puppeteer 渲染) | 导出 PDF 中文正常显示,不再降级 `?` |

---

## 三、B类 · 基础设施降级项(中间件未就绪时降级)

> 说明:以下逻辑在对应中间件缺失时自动降级放行 / 兜底,保证链路可运行。提供真实中间件实例后可恢复严格行为并实测。

| 中间件 | 涉及文件 | 降级行为 | 恢复条件 |
| --- | --- | --- | --- |
| **Redis 延迟关单队列** | `apps/api/src/modules/payment/payment.service.ts`(15 分钟延迟关单) | 无 Redis 时依赖 `setInterval` 60s 定时兜底扫描 `(status, expire_at)` 关闭过期 PENDING 订单 | 接入真实 Redis,启用 ZSET 延迟队列精确到期触发,兜底扫描作为补偿 |
| **支付回调并发一致性** | `apps/api/src/modules/payment/payment.service.ts`(`handleCallback` 事务 + `uk_channel_trade_no` 复合唯一约束 + P2002 幂等) | 单实例已具备幂等;多实例并发需真实 DB 唯一约束生效 | 部署真实 MySQL 并确认 `uk_channel_trade_no` 唯一索引已建,实测并发重复回调幂等 |

---

## 四、C类 · 后续渠道扩展(V1.1+ 规划项)

> 说明:当前仅实现微信支付 mock 通道,以下渠道为后续扩展位,接口已预留适配器模式。

| 项 | 涉及文件 | 现状 | 扩展动作 |
| --- | --- | --- | --- |
| **支付宝渠道适配器** | `apps/api/src/modules/payment/pay-channel.adapter.ts`(`PayChannelAdapter` 接口) | 未实现 | 新增 `AlipayAdapter` 实现 `prepay/verifySign/refund`,注册到 `payment.module` |
| **余额/会员权益渠道** | `apps/api/src/modules/payment/payment.service.ts`(多态 `biz_type=3` 会员) | 下单链路已通,渠道未接 | 补齐余额扣减渠道适配器与会员权益发放 |
| **报告海报 OSS 上传** | `apps/api/src/modules/report/report.service.ts` | 沿用阶段1 `posterUrl:null` blocked | 见阶段1 A类 OSS 海报项,联调后回填 |

---

## 四点五、C2类 · 前端付费闭环 blocked / mock 项(T2-08 / T2-09 / T2-11)

> 说明:阶段2 前端付费闭环(P30 套餐选择 → 收银台 → P31 支付结果 → 报告解锁)已交付并通过 L1 类型门禁。以下为联调前需人工补齐 / 移除降级兜底的项。

| 项 | 涉及文件 | 现状(降级/mock) | 恢复方法 | 验证方式 |
| --- | --- | --- | --- | --- |
| **订单列表 / 详情接口** | `apps/web/src/hooks/usePayment.ts`(`useOrders` / `useOrder`,`ORDER_CACHE_KEY='iq_orders_cache'`) | 后端暂无 `GET /payments/orders` 与 `GET /payments/orders/:id`,当前用 localStorage 本地缓存拼装列表/详情,过期项前端置 CLOSED | 后端补齐订单查询接口后,`useOrders`/`useOrder` 改走真实 API,删除 localStorage 缓存拼装与 `upsertOrderCache`/`markOrderPaid` 兜底 | 订单页数据来自后端,刷新后仍在 |
| **套餐列表 mock fallback** | `apps/web/src/hooks/useMembership.ts`(`usePlans`/`usePlan` 的 `mockPlans()`) | 接口异常时回退内置 mock 套餐(basic 单次解锁 990分 / year 年度会员 9900分) | 确认后端 `GET /memberships/plans` 就绪且返回结构对齐后,移除 `mockPlans()` fallback | 套餐页数据来自后端,断网时不再出现 mock 套餐 |
| **支付 / 解锁 mock 兜底** | `apps/web/src/pages/app/CheckoutPage.tsx`(`usePayOrder`→`markOrderPaid`→`useUnlockReport`) | 依赖 `PrepayResult.mock` 兜底,本地直接置已支付并解锁,未走真实渠道跳转 | 后端接入真实微信支付后(见 A类),前端按 `payParams` 跳转真实收银,回调后再解锁 | 真实支付回跳,`sections[].locked` 由后端解锁后翻转 |
| **RequirePaid 整页门禁未启用** | `apps/web/src/components/guards/RequirePaid.tsx` | 守卫已创建但未在 `routes.tsx` 挂载;报告页(`ReportPage`)保留锁态段内引导,而非整页拦截 | 如需整页付费拦截,在 `/app/report/:id` 路由用 `RequirePaid` 包裹;但会挡住含锁段的基础报告展示,需产品权衡 | 未解锁访问报告页跳转 `/pricing` |

---

## 五、门禁与验证记录

| 门禁 | 命令 | 结果 |
| --- | --- | --- |
| L1 编译 | `npx tsc -p tsconfig.json --noEmit` | ✅ 通过(TSC_OK) |
| L2 单测 | `npx jest payment.service.spec` | ✅ 13/13 通过 |
| Lint | `eslint` | ⚠️ 仓库未提供 ESLint 配置文件,门禁不适用(blocked · 环境项) |
| L1 前端类型(T2-08/09/11) | `tsc --noEmit -p tsconfig.json`(根 workspace) | ✅ 通过(TSC_PASS,新增 6 个 .tsx/.ts 全绿) |

> 备注:阶段2 后端核心链路已判定通过。上述 blocked 项均为环境/资质/中间件补齐项,不阻断门禁,联调或部署前按表逐项恢复即可。