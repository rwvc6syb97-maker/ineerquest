# 阶段1 · 人工调试待办清单

> 项目:InnerQuest(MBTI 职业规划平台)
> 范围:阶段1(MVP 免费首发 T1-01 ~ T1-23)完成后所有需要人工介入调试/补齐的问题

---

## 一、概述

阶段1(MVP 免费首发)已通过**验证检查点1**:

- L1 ~ L4 静态/契约/链路校验全绿;
- 测评 → 计分 → 报告 → 职业规划核心链路贯通;
- 前端页面按前端页面清单实现替换到位。

本文档列出的条目均为 **"环境非阻断"(blocked / mock)** 项:

- **不影响阶段1门禁**,阶段1 已判定通过;
- 但在**真实联调**或**实际部署**前,需人工补齐相应外部资质、中间件实例与真实接口。

各条目均对应已核验的真实代码位置,联调补齐后即可删除对应的降级兜底逻辑。

---

## 二、A类 · 外部资质与凭证(最高优先级 · 阻断真实连通)

> 说明:以下能力当前均走 mock / 兜底 / 占位实现。提供真实凭证与实例并回填后,即可解除对应 blocked 项并复验真实链路。

| 项 | 涉及文件 | 占位或开关 | 恢复方法 | 验证方式 |
| --- | --- | --- | --- | --- |
| **短信通道** | `apps/api/src/modules/user/auth/sms.provider.ts`、`apps/api/src/modules/user/auth/sms-code.service.ts` | 开关 `SMS_PROVIDER_ENABLED`(默认 `false` → mock) | 置 `true` 并接入阿里云 `dysmsapi` / 腾讯云 `sms` SDK,回填 AccessKey / 签名 / 模板 ID | 真实手机号收到验证码,验证码校验链路通过 |
| **微信 OAuth** | `apps/api/src/modules/user/auth/wechat-oauth.provider.ts`、`apps/api/src/modules/user/auth/auth.service.ts`(失败降级手机号 `fallback:true`)、前端 `apps/web/src/pages/auth/LoginPage.tsx`(TODO blocked 跳转微信授权) | 开关 `WECHAT_OAUTH_ENABLED` | 提供真实 AppID / AppSecret + 回调地址,前端补齐跳转微信授权 | 微信扫码登录成功,不再走手机号 fallback |
| **JWT 密钥** | `apps/api/src/modules/user/auth/token.service.ts`(自实现 HS256) | 密钥 env 占位 `CHANGE_ME` | 生成并回填强随机密钥(生产禁用占位) | JWT 签发 / 校验 / 刷新链路通过 |
| **LLM 网关(报告深度解读)** | `apps/api/src/modules/report/report.service.ts`(TODO blocked 接入 `LLMGateway`,无实例返回 `undefined` 走兜底)、`apps/api/src/modules/report/report.constants.ts`(`DEEP_SECTION_FALLBACK` 占位文案)、`apps/api/src/modules/report/report-content.builder.ts`、`apps/api/src/modules/report/llm-gateway.module.ts`(阶段0 空壳,待 T3-01 ~ T3-03) | 无 LLM 实例 → 兜底文案 | 接入真实 LLM 网关实例并回填密钥 | 报告深度解读返回真实生成内容,不再走 `DEEP_SECTION_FALLBACK` |
| **OSS 海报** | `apps/api/src/modules/report/report.service.ts`(`posterUrl:null` TODO blocked 海报生成 / 上传 OSS) | `posterUrl` 恒为 `null` | 补齐海报生成并上传阿里云 OSS,回填 OSS 凭证 | 报告 `posterUrl` 返回可访问的真实 URL |

---

## 三、B类 · 基础设施降级项(中间件未就绪时降级)

> 说明:以下逻辑在对应中间件缺失时自动降级放行 / 兜底,保证 MVP 可运行。提供真实中间件实例后可恢复严格行为并实测。

| 中间件 | 涉及文件 | 降级行为 | 恢复条件 |
| --- | --- | --- | --- |
| **Redis 报告日配额**(每日 3 份 `40003`) | `apps/api/src/modules/report/report.service.ts`(`consumeDailyQuota`) | 无 Redis 时降级放行(标 blocked) | 接入真实 Redis,恢复日配额计数并实测超限抛 `40003` |
| **Redis 测评草稿快照** | `apps/api/src/modules/assessment/assessment.service.ts` | 用于断点续答加速,失败时降级不阻断 | 接入真实 Redis,验证草稿快照读写与断点续答 |
| **Redis 令牌桶限流**(100/分 `429` + `90001`) | `apps/api/src/common/interceptors/rate-limit.interceptor.ts`(T1-22 已实现 Lua) | 无 Redis 实例 → 待实测 | 接入真实 Redis,实测 100/分 限流抛 `429` / `90001` |
| **ClickHouse / MQ 埋点** | `apps/api/src/modules/analytics/analytics.service.ts` | MQ 无实例 → 降级进程内直写;ClickHouse 未就绪 → 降级跳过 / MySQL 聚合 | 接入真实 MQ 与 ClickHouse,恢复异步埋点与 ClickHouse 聚合 |
| **职业检索 FULLTEXT → LIKE 降级** | `apps/api/src/modules/career/career.service.ts` | MySQL 无 FT 索引时降级 `LIKE` 查询;ES 延后至 V1.1 | 建立 MySQL FULLTEXT 索引恢复全文检索(ES 方案 V1.1) |

---

## 四、C类 · 前端 mock fallback(联调真实接口后删除)

> 说明:以下 Hook 在真实接口未联通时提供 mock 兜底数据,保证前端可独立开发预览。联调真实接口需删除对应 fallback。

| 涉及文件 | mock 内容 | 联调删除动作 |
| --- | --- | --- |
| `apps/web/src/hooks/useAssessment.ts` | `MOCK_QUESTIONS`(40 题兜底) | 联调真实接口后删除 fallback |
| `apps/web/src/hooks/useReport.ts` | `mockReport` 兜底 | 联调真实接口后删除 fallback |
| `apps/web/src/hooks/useCareer.ts` | `MOCK_CAREERS` / `mockDetail` 兜底 | 联调真实接口后删除 fallback |

---

## 五、D类 · 前端功能占位(阶段后续增强)

- [ ] **Chart.js 可视化**:`apps/web/src/components/charts/DimensionCharts.tsx`(当前零依赖 SVG 替代,如需精细交互可安装 `chart.js` / `react-chartjs-2` 替换)
- [ ] **海报 PNG 导出**:`apps/web/src/pages/app/SharePage.tsx`(TODO blocked 接后端 poster 或 `html2canvas`)
- [ ] **Token 存储**:`apps/web/src/api/token.ts`(可切换为 `httpOnly` Cookie,需后端配合)

---

## 六、已在验证检查点1 修复的真实源码缺陷(仅作记录 · 已修复无需处理)

> 验证检查点1 中共修复 5 处前端 API 路径 / 参数缺陷:

- `apps/web/src/api/client.ts`:refresh 路径 `/user/auth` → `/auth` → **已修复**
- `apps/web/src/api/user.api.ts`:`login` / `logout` / `getProfile` 路径错配 → **已修复**
- `apps/web/src/api/career.api.ts`:`recommendCareers` 参数 `mbtiType` → `reportId`,并补齐返回结构映射 → **已修复**
- `apps/web/src/hooks/useCareer.ts`:改用 `reportId`,补充无报告兜底 → **已修复**
- `apps/web/src/pages/app/CareerListPage.tsx`:改读 `reportId` 参数 → **已修复**

---

## 七、进入阶段2前的检查清单

> 汇总进入阶段2(付费拓展)业务开发实际需要的前置项:

- [ ] 补齐**微信支付资质**(阶段2 T2 依赖)
- [ ] 接入真实 **Redis**,验证配额 / 限流 / 黑名单
- [ ] 接入真实 **DB**,端到端验证测评 → 计分 → 报告链路
- [ ] 删除前端**三处 mock fallback**(`useAssessment` / `useReport` / `useCareer`)
- [ ] 补齐 **LLM 网关兜底**(报告深度解读)

---

> **恢复条件说明**:一旦提供对应外部资质、中间件实例及真实接口凭证并回填,即可解除对应 blocked / mock 项,删除降级兜底逻辑,并复验真实链路全绿。